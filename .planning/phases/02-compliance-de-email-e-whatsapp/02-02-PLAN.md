---
phase: 02-compliance-de-email-e-whatsapp
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - convex/outreach.ts
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]
nyquist_compliant: true

must_haves:
  truths:
    - "outreach.draft recusa endereço suprimido antes de gastar a chamada de IA (erro pt-BR)"
    - "outreach.send recusa endereço suprimido antes do POST ao Resend"
    - "O body persistido pelo draft já contém o rodapé de opt-out com o link de unsubscribe"
    - "send garante o unsubscribeToken (backfill on-demand para rows legadas), re-garante o rodapé e envia os headers List-Unsubscribe / List-Unsubscribe-Post"
    - "outreach.suppress registra supressão manual org-scoped (source manual)"
  artifacts:
    - path: "convex/outreach.ts"
      provides: "checagem de supressão em draft/send, token+rodapé no upsertDraft, headers no send, mutation suppress, setUnsubscribeToken"
      contains: "List-Unsubscribe"
  key_links:
    - from: "convex/outreach.ts:draft"
      to: "internal.suppressions.isSuppressed"
      via: "ctx.runQuery antes de writeEmail"
      pattern: "internal\\.suppressions\\.isSuppressed"
    - from: "convex/outreach.ts:send"
      to: "Resend headers"
      via: "campo headers no JSON do POST /emails"
      pattern: "List-Unsubscribe-Post"
    - from: "convex/outreach.ts:upsertDraft"
      to: "convex/lib/compliance.ts:optOutFooter"
      via: "anexa rodapé ao body persistido"
      pattern: "optOutFooter"
    - from: "convex/outreach.ts:send"
      to: "internal.outreach.setUnsubscribeToken"
      via: "backfill on-demand do token em rows sem unsubscribeToken"
      pattern: "setUnsubscribeToken"
---

<objective>
Fazer de `convex/outreach.ts` o ponto único e não-contornável de compliance de email (COMP-01/02/03). Depende do plano 02-01 (schema + helpers + suppressions). Este plano: (1) checa supressão em `draft` e `send`; (2) faz o `upsertDraft` gerar/persistir o `unsubscribeToken` e injetar o rodapé de opt-out no body salvo (cobre o fluxo manual de copiar/enviar e o `markSent`); (3) no `send`, re-garante token (backfill on-demand p/ rows legadas), re-garante o rodapé e adiciona os headers `List-Unsubscribe`/`List-Unsubscribe-Post` no payload do Resend; (4) adiciona a mutation autenticada `outreach.suppress`.

Purpose: Mesmo que a IA falhe no parse (fallback outreachAi.ts:88) ou que o usuário edite o corpo, o email enviado SEMPRE sai com rodapé + headers, e um endereço suprimido nunca recebe draft nem envio.
Output: outreach.ts com supressão + token + rodapé + headers + suppress.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-compliance-de-email-e-whatsapp/02-CONTEXT.md
@.planning/phases/02-compliance-de-email-e-whatsapp/02-RESEARCH.md
@.planning/phases/02-compliance-de-email-e-whatsapp/02-01-SUMMARY.md

<interfaces>
<!-- Contratos do plano 02-01 (wave 1) e do estado atual de outreach.ts. Use direto. -->

Do plano 02-01 (importar):
```ts
import { normalizeEmail } from "./lib/domain";
import { optOutFooter, senderIdentityFrom } from "./lib/compliance";
import { LANG } from "./lib/outreachAi";
import { addSuppression } from "./suppressions";
import type { Doc } from "./_generated/dataModel";
// e, para actions: ctx.runQuery(internal.suppressions.isSuppressed, { email, orgId }) → boolean
```

Estado atual `convex/outreach.ts`:
- `upsertDraft` (internalMutation, 73-105): args {orgId, leadId, subject, body, previewToken}; faz query by_lead → patch ou insert com channel "email", status "draft". NÃO gera token nem rodapé hoje.
- `draft` (action, 108-133): carrega lead via internal.leads.getInternal; checa `lead.emailable`; chama ensureForLead + writeEmail; chama internal.outreach.upsertDraft.
- `send` (action, 156-185): carrega lead; exige lead.emailable e lead.email; `row = ctx.runQuery(api.outreach.getForLead,{leadId})` (Doc<"outreach"> completo, tem `_id`, `body`, `subject`, `unsubscribeToken`); monta payload Resend `{ from, to, subject, text: row.body }` (172-176); chama api.outreach.markSent.
- `from = process.env.RESEND_FROM` (169). Env `CONVEX_SITE_URL` é built-in do Convex (mesma usada no comentário de http.ts).

Token: `crypto.randomUUID().replace(/-/g, "")` (padrão previews.ts).
Erro pt-BR de supressão (locked, CONTEXT.md): `"Este contato pediu para não ser contatado (opt-out)."`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supressão no draft + mutation outreach.suppress (COMP-01)</name>
  <files>convex/outreach.ts</files>
  <read_first>
    - convex/outreach.ts (draft action 108-133; padrão de requireOrgId + ownership de markSent 136-153)
    - convex/suppressions.ts (addSuppression, isSuppressed — do plano 02-01)
    - convex/lib/domain.ts (normalizeEmail)
  </read_first>
  <action>
    1. Adicionar imports no topo de `convex/outreach.ts`:
    ```ts
    import { normalizeEmail } from "./lib/domain";
    import { addSuppression } from "./suppressions";
    ```
    (o arquivo já importa `mutation`, `internal`, `api`, `requireOrgId`.)

    2. Em `draft`, DEPOIS de validar `lead.emailable` e ANTES de `ctx.runMutation(internal.previews.ensureForLead,...)`, checar supressão (só se houver email):
    ```ts
    if (lead.email) {
      const suppressed = await ctx.runQuery(internal.suppressions.isSuppressed, {
        email: normalizeEmail(lead.email),
        orgId,
      });
      if (suppressed) throw new Error("Este contato pediu para não ser contatado (opt-out).");
    }
    ```

    3. Adicionar a mutation autenticada `suppress` (org-scoped, source "manual"). A UI que a chama é da Fase 3 — aqui só a mutation existe:
    ```ts
    /** Registrar supressão manual ("respondeu stop"). Org-scoped. UI na Fase 3. */
    export const suppress = mutation({
      args: { leadId: v.id("leads") },
      handler: async (ctx, { leadId }) => {
        const orgId = await requireOrgId(ctx);
        const lead = await ctx.db.get(leadId);
        if (!lead || lead.orgId !== orgId) throw new Error("Lead não encontrado");
        if (!lead.email) throw new Error("Lead sem email para suprimir.");
        await addSuppression(ctx, {
          email: normalizeEmail(lead.email),
          orgId,
          source: "manual",
          leadId,
        });
        return { suppressed: true };
      },
    });
    ```
  </action>
  <verify>
    <automated>npx convex codegen && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "internal.suppressions.isSuppressed" convex/outreach.ts`
    - `grep -q "export const suppress = mutation" convex/outreach.ts`
    - `grep -q "Este contato pediu para não ser contatado" convex/outreach.ts`
    - `grep -q "source: \"manual\"" convex/outreach.ts`
    - `pnpm typecheck` exit 0 e `pnpm test` verde (regressão)
  </acceptance_criteria>
  <done>draft lança erro pt-BR para email suprimido antes de gastar IA; outreach.suppress grava supressão manual org-scoped via addSuppression.</done>
</task>

<task type="auto">
  <name>Task 2: upsertDraft gera unsubscribeToken + injeta rodapé; internal setUnsubscribeToken (COMP-02/03 lado draft)</name>
  <files>convex/outreach.ts</files>
  <read_first>
    - convex/outreach.ts (upsertDraft 73-105 — a reescrever)
    - convex/lib/compliance.ts (optOutFooter, senderIdentityFrom)
    - convex/lib/outreachAi.ts (LANG — agora exportado)
    - convex/previews.ts (padrão crypto.randomUUID().replace(/-/g,""))
  </read_first>
  <action>
    1. Imports adicionais:
    ```ts
    import { optOutFooter, senderIdentityFrom } from "./lib/compliance";
    import { LANG } from "./lib/outreachAi";
    import type { Doc } from "./_generated/dataModel";
    ```

    2. Adicionar dois helpers de módulo no topo de outreach.ts (fora dos handlers):
    ```ts
    function buildUnsubscribeUrl(token: string): string {
      return `${process.env.CONVEX_SITE_URL}/unsubscribe?token=${token}`;
    }
    /** Anexa o rodapé de opt-out se ainda não estiver presente (marcador = a URL única). */
    function withOptOutFooter(body: string, lead: Doc<"leads"> | null, token: string): string {
      const url = buildUnsubscribeUrl(token);
      if (body.includes(url)) return body; // idempotente
      const sender = senderIdentityFrom(process.env.RESEND_FROM ?? "Osprano");
      const lang = LANG[lead?.countryCode ?? ""] ?? "English";
      return `${body}${optOutFooter(lang, url, sender)}`;
    }
    ```

    3. Reescrever `upsertDraft` para: buscar o lead (p/ countryCode→LANG), reusar o token existente ou gerar um novo, e persistir o body JÁ com rodapé + o unsubscribeToken:
    ```ts
    export const upsertDraft = internalMutation({
      args: { orgId: v.string(), leadId: v.id("leads"), subject: v.string(), body: v.string(), previewToken: v.string() },
      handler: async (ctx, args) => {
        const lead = await ctx.db.get(args.leadId);
        const existing = await ctx.db
          .query("outreach")
          .withIndex("by_lead", (q) => q.eq("leadId", args.leadId))
          .first();
        const unsubscribeToken = existing?.unsubscribeToken ?? crypto.randomUUID().replace(/-/g, "");
        const body = withOptOutFooter(args.body, lead, unsubscribeToken);
        if (existing) {
          await ctx.db.patch(existing._id, {
            subject: args.subject, body, previewToken: args.previewToken, unsubscribeToken, status: "draft",
          });
          return existing._id;
        }
        return await ctx.db.insert("outreach", {
          orgId: args.orgId, leadId: args.leadId, channel: "email",
          subject: args.subject, body, previewToken: args.previewToken, unsubscribeToken, status: "draft",
        });
      },
    });
    ```

    4. Adicionar a internal mutation de backfill usada pelo send:
    ```ts
    export const setUnsubscribeToken = internalMutation({
      args: { outreachId: v.id("outreach"), unsubscribeToken: v.string() },
      handler: async (ctx, { outreachId, unsubscribeToken }) => {
        await ctx.db.patch(outreachId, { unsubscribeToken });
      },
    });
    ```
  </action>
  <verify>
    <automated>npx convex codegen && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "crypto.randomUUID" convex/outreach.ts`
    - `grep -q "withOptOutFooter" convex/outreach.ts`
    - `grep -q "unsubscribeToken" convex/outreach.ts`
    - `grep -q "export const setUnsubscribeToken = internalMutation" convex/outreach.ts`
    - `pnpm typecheck` exit 0 e `pnpm test` verde
  </acceptance_criteria>
  <done>upsertDraft persiste unsubscribeToken e body com rodapé (idempotente); setUnsubscribeToken existe como internalMutation para backfill.</done>
</task>

<task type="auto">
  <name>Task 3: send — supressão + backfill de token + re-garantia do rodapé + headers Resend (COMP-01/02/03)</name>
  <files>convex/outreach.ts</files>
  <read_first>
    - convex/outreach.ts (send 156-185; payload Resend 172-176)
    - .planning/phases/02-compliance-de-email-e-whatsapp/02-RESEARCH.md (§Pattern 4 backfill; §Code Examples headers Resend; §Pitfall 2/4)
  </read_first>
  <action>
    Editar o handler de `send`, entre a validação de `row?.subject/body` e o `fetch` ao Resend.

    1. Checagem de supressão (lead.email já é exigido acima):
    ```ts
    const suppressed = await ctx.runQuery(internal.suppressions.isSuppressed, {
      email: normalizeEmail(lead.email),
      orgId,
    });
    if (suppressed) throw new Error("Este contato pediu para não ser contatado (opt-out).");
    ```

    2. Backfill on-demand do token (rows legadas / seed demo sem unsubscribeToken) + re-garantia do rodapé:
    ```ts
    let unsubscribeToken = row.unsubscribeToken;
    if (!unsubscribeToken) {
      unsubscribeToken = crypto.randomUUID().replace(/-/g, "");
      await ctx.runMutation(internal.outreach.setUnsubscribeToken, {
        outreachId: row._id, unsubscribeToken,
      });
    }
    const unsubscribeUrl = `${process.env.CONVEX_SITE_URL}/unsubscribe?token=${unsubscribeToken}`;
    let body = row.body!;
    if (!body.includes(unsubscribeUrl)) {
      const lang = LANG[lead.countryCode] ?? "English";
      body = `${body}${optOutFooter(lang, unsubscribeUrl, senderIdentityFrom(from))}`;
    }
    ```

    3. Trocar o corpo do fetch ao Resend por (usa `body` garantido + adiciona `headers`):
    ```ts
    body: JSON.stringify({
      from,
      to: lead.email,
      subject: row.subject,
      text: body,
      headers: {
        "List-Unsubscribe": `<${unsubscribeUrl}>`,
        "List-Unsubscribe-Post": "List-Unsubscribe=One-Click",
      },
    }),
    ```
    Manter angle brackets em List-Unsubscribe (Pitfall 2) e o valor exato "List-Unsubscribe=One-Click" (RFC 8058). Sem mailto nesta fase.
  </action>
  <verify>
    <automated>npx convex codegen && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "internal.outreach.setUnsubscribeToken" convex/outreach.ts`
    - `grep -q '"List-Unsubscribe": `<${unsubscribeUrl}>`' convex/outreach.ts || grep -q "List-Unsubscribe" convex/outreach.ts`
    - `grep -q "List-Unsubscribe=One-Click" convex/outreach.ts`
    - `grep -c "internal.suppressions.isSuppressed" convex/outreach.ts` retorna 2 (draft + send)
    - `pnpm typecheck` exit 0 e `pnpm test` verde
  </acceptance_criteria>
  <done>send recusa suprimidos, gera token faltante on-demand, re-garante o rodapé e envia os headers List-Unsubscribe/List-Unsubscribe-Post — independente do que a IA ou a edição do usuário produziram.</done>
</task>

</tasks>

<verification>
- `npx convex codegen && pnpm typecheck && pnpm lint && pnpm test` verde.
- `internal.suppressions.isSuppressed` referenciado 2x (draft + send).
- Payload do Resend inclui `headers` com List-Unsubscribe e List-Unsubscribe-Post.
- Manual/smoke (pós-fase, requer deployment): `send` num lead com row legada (sem unsubscribeToken) não quebra e o header sai válido.
- Nenhum outro plano toca convex/outreach.ts (exclusivo desta wave).
</verification>

<success_criteria>
- Supressão bloqueia draft e send (erro pt-BR).
- Body do draft e o email enviado sempre carregam o rodapé de opt-out.
- Headers List-Unsubscribe/List-Unsubscribe-Post sempre no payload; token backfillado on-demand.
- outreach.suppress registra supressão manual.
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-compliance-de-email-e-whatsapp/02-02-SUMMARY.md`.
</output>
