---
phase: 02-compliance-de-email-e-whatsapp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/lib/domain.ts
  - convex/lib/compliance.ts
  - convex/lib/outreachAi.ts
  - convex/suppressions.ts
  - tests/domain.test.ts
  - tests/compliance.test.ts
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]
nyquist_compliant: true

must_haves:
  truths:
    - "A tabela suppressions existe no schema com índices by_email e by_org_email"
    - "normalizeEmail retorna forma canônica lowercase/trim; hasWaOptIn reflete presença de waOptInAt"
    - "optOutFooter retorna copy por idioma (English/Dutch/Swedish/Norwegian) e sempre inclui a URL de unsubscribe"
    - "suppressions é idempotente (não duplica email+org); isSuppressed casa por org OU global; unsubscribeByToken é no-leak (no-op para token desconhecido)"
    - "outreach ganha unsubscribeToken + índice by_unsub_token; leads ganham waOptInAt/waOptInSource; events.type inclui wa_opt_in"
  artifacts:
    - path: "convex/schema.ts"
      provides: "tabela suppressions + campos/índices novos em outreach/leads/events"
      contains: "suppressions"
    - path: "convex/lib/domain.ts"
      provides: "normalizeEmail + hasWaOptIn (puros, testáveis)"
      contains: "export function normalizeEmail"
    - path: "convex/lib/compliance.ts"
      provides: "optOutFooter por idioma + senderIdentityFrom (puros, testáveis)"
      contains: "export function optOutFooter"
    - path: "convex/lib/outreachAi.ts"
      provides: "LANG exportado para reuso no rodapé"
      contains: "export const LANG"
    - path: "convex/suppressions.ts"
      provides: "addSuppression (helper) + add/isSuppressed/unsubscribeByToken"
      contains: "export const unsubscribeByToken"
    - path: "tests/compliance.test.ts"
      provides: "casos de optOutFooter por idioma + senderIdentityFrom"
      contains: "optOutFooter"
  key_links:
    - from: "convex/suppressions.ts:unsubscribeByToken"
      to: "outreach.by_unsub_token"
      via: "lookup do outreach pelo token para achar org+lead+email"
      pattern: "by_unsub_token"
    - from: "convex/suppressions.ts"
      to: "convex/lib/domain.ts:normalizeEmail"
      via: "normaliza o email do lead antes de gravar supressão"
      pattern: "normalizeEmail"
---

<objective>
Construir toda a fundação de dados e lógica pura da Fase 2 num único plano de wave 1, para que os planos paralelos de wave 2 (envio de email, endpoint de unsubscribe, opt-in de WhatsApp) NÃO precisem tocar `convex/schema.ts` nem os helpers puros — evitando o conflito transitório de working tree observado na Fase 1. Este plano adiciona: (1) a tabela `suppressions` + campos/índices novos em `outreach`/`leads`/`events`; (2) os helpers puros `normalizeEmail`, `hasWaOptIn`, `optOutFooter`, `senderIdentityFrom` com testes `node:test`; (3) o módulo `convex/suppressions.ts` com o helper `addSuppression` (idempotente) e as funções registradas `add`/`isSuppressed`/`unsubscribeByToken`.

Purpose: Nenhum plano de wave 2 modifica schema.ts ou os helpers — cada um só importa daqui. Toda a lógica de compliance testável nasce coberta.
Output: schema estendido + 4 helpers puros testados + módulo suppressions completo.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-compliance-de-email-e-whatsapp/02-CONTEXT.md
@.planning/phases/02-compliance-de-email-e-whatsapp/02-RESEARCH.md

<interfaces>
<!-- Contratos exatos do código atual. Use direto, sem explorar. -->

Schema atual relevante (`convex/schema.ts`):
```ts
// leads: defineTable({ ... , stage, stageUpdatedAt: v.number(), saved: v.optional(v.boolean()), meetingAt, meetingNote, fetchedAt }) .index("by_org",...).index("by_org_stage",...).index("by_org_place",...)
// outreach: defineTable({ orgId, leadId, channel, subject, body, status, previewToken: v.optional(v.string()), sentAt, openedAt }) .index("by_org",...).index("by_lead",...)
// events.type: v.union(v.literal("preview_open"), v.literal("email_sent"), v.literal("reply"), v.literal("stage_change"))
```

Padrão de token já usado (`convex/previews.ts:65,92`): `crypto.randomUUID().replace(/-/g, "")`.

LANG atual (`convex/lib/outreachAi.ts:5-11`) — HOJE é `const` privado; trocar para `export const`:
```ts
const LANG: Record<string, string> = { GB: "English", IE: "English", NL: "Dutch", SE: "Swedish", NO: "Norwegian" };
```

Padrão de teste puro (`tests/domain.test.ts`) — import direto com extensão `.ts`, `node:test`:
```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { normalizeEmail, hasWaOptIn } from "../convex/lib/domain.ts";
```

Padrão de mutation/query internas + `MutationCtx` (`convex/model/workspace.ts` usa funções puras que recebem `ctx`; `convex/leads.ts:178` mostra `internalQuery`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender o schema (suppressions + campos/índices em outreach/leads/events)</name>
  <files>convex/schema.ts</files>
  <read_first>
    - convex/schema.ts (ver a definição das tabelas leads/outreach/events e o estilo de índices compostos por org, ex.: by_org_place)
    - .planning/phases/02-compliance-de-email-e-whatsapp/02-CONTEXT.md (§COMP-01/02/04 — shapes exatos)
    - .planning/phases/02-compliance-de-email-e-whatsapp/02-RESEARCH.md (§Pitfall 6 — rodar codegen após editar schema)
  </read_first>
  <action>
    Editar `convex/schema.ts`:

    1. Adicionar a tabela `suppressions` (dentro do objeto de `defineSchema`, ao lado das outras):
    ```ts
    suppressions: defineTable({
      email: v.string(), // normalizado (lowercase/trim) via normalizeEmail — SEMPRE gravado normalizado
      orgId: v.optional(v.string()), // undefined = supressão global (todas as orgs)
      source: v.string(), // "unsubscribe_link" | "manual" | "reply_stop"
      leadId: v.optional(v.id("leads")),
      at: v.number(),
    })
      .index("by_email", ["email"])
      .index("by_org_email", ["orgId", "email"]),
    ```

    2. Na tabela `outreach`, adicionar o campo (perto de `previewToken`) e o índice:
    ```ts
    unsubscribeToken: v.optional(v.string()), // token dedicado do unsubscribe (NÃO reutilizar previewToken)
    ```
    e no encadeamento de índices adicionar: `.index("by_unsub_token", ["unsubscribeToken"])`

    3. Na tabela `leads`, adicionar (perto de `meetingAt`/`meetingNote`):
    ```ts
    // WhatsApp opt-in (COMP-04) — só com registro explícito, nunca por estágio de Kanban
    waOptInAt: v.optional(v.number()),
    waOptInSource: v.optional(v.string()), // "replied_email" | "phone_call" | "in_person"
    ```

    4. No union `events.type`, adicionar o literal `v.literal("wa_opt_in")` como último membro.

    Depois de salvar, rodar `npx convex codegen` para regenerar `convex/_generated/*` (Pitfall 6: adicionar campos ao schema NÃO faz backfill; rows antigas retornam undefined nesses campos — isso é esperado e tratado on-demand no plano 02-02).
  </action>
  <verify>
    <automated>npx convex codegen && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "suppressions: defineTable" convex/schema.ts`
    - `grep -q "by_unsub_token" convex/schema.ts`
    - `grep -q "waOptInAt" convex/schema.ts && grep -q "waOptInSource" convex/schema.ts`
    - `grep -q 'v.literal("wa_opt_in")' convex/schema.ts`
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>Schema tem a tabela suppressions com by_email/by_org_email, outreach tem unsubscribeToken+by_unsub_token, leads têm waOptInAt/waOptInSource, events.type inclui "wa_opt_in", e o codegen/typecheck passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Helpers puros — normalizeEmail, hasWaOptIn, optOutFooter, senderIdentityFrom (+ testes)</name>
  <files>convex/lib/domain.ts, convex/lib/compliance.ts, convex/lib/outreachAi.ts, tests/domain.test.ts, tests/compliance.test.ts</files>
  <read_first>
    - convex/lib/domain.ts (padrão de export puro SEM imports de Convex — adicionar na seção "Markets & compliance")
    - convex/lib/outreachAi.ts (linha 5 — LANG é `const` privado; trocar para `export const`, sem outra mudança no arquivo)
    - tests/domain.test.ts (padrão node:test/assert strict, import com extensão .ts)
  </read_first>
  <behavior>
    Escrever os testes PRIMEIRO (RED), depois implementar (GREEN).

    normalizeEmail(email: string): string
    - normalizeEmail("  Info@Business.COM ") === "info@business.com"
    - normalizeEmail("a@b.com") === "a@b.com"

    hasWaOptIn(lead: { waOptInAt?: number | null }): boolean
    - hasWaOptIn({}) === false
    - hasWaOptIn({ waOptInAt: undefined }) === false
    - hasWaOptIn({ waOptInAt: 0 }) === false
    - hasWaOptIn({ waOptInAt: 1700000000000 }) === true

    optOutFooter(lang, unsubscribeUrl, senderIdentity): string
    - optOutFooter("English", "https://x.convex.site/unsubscribe?token=abc", "Ana") inclui "abc" E "Ana" E começa com "\n\n—\n"
    - optOutFooter("Dutch", url, sender) inclui a URL e é distinto do inglês (contém "Afmelden")
    - optOutFooter("Swedish", url, sender) contém "Avregistrera"
    - optOutFooter("Norwegian", url, sender) contém "Meld deg av"
    - optOutFooter("Klingon", url, sender) cai no fallback inglês (contém a URL)

    senderIdentityFrom(resendFrom: string): string
    - senderIdentityFrom("Ana <ana@osprano.com>") === "Ana"
    - senderIdentityFrom("ana@osprano.com") === "ana@osprano.com"
    - senderIdentityFrom("  Team Osprano  <hi@osprano.com> ") === "Team Osprano"
  </behavior>
  <action>
    1. `convex/lib/domain.ts` — adicionar (seção Markets & compliance):
    ```ts
    /** Forma canônica para casar supressão (Convex não tem índice case-insensitive). */
    export function normalizeEmail(email: string): string {
      return email.trim().toLowerCase();
    }

    /** Gate de WhatsApp (COMP-04): só há opt-in se houver timestamp registrado. */
    export function hasWaOptIn(lead: { waOptInAt?: number | null }): boolean {
      return typeof lead.waOptInAt === "number" && lead.waOptInAt > 0;
    }
    ```

    2. `convex/lib/compliance.ts` (NOVO arquivo — puro, sem imports de Convex):
    ```ts
    /** Rodapé de opt-out injetado por código (COMP-03). Copy honesta, sem hype. */
    const FOOTER_COPY: Record<string, (sender: string, url: string) => string> = {
      English: (s, url) => `\n\n—\nSent by ${s}. Don't want to hear from us again? Unsubscribe: ${url}`,
      Dutch: (s, url) => `\n\n—\nVerzonden door ${s}. Wilt u niets meer van ons ontvangen? Afmelden: ${url}`,
      Swedish: (s, url) => `\n\n—\nSkickat av ${s}. Vill du inte höra från oss igen? Avregistrera dig: ${url}`,
      Norwegian: (s, url) => `\n\n—\nSendt av ${s}. Vil du ikke høre fra oss igjen? Meld deg av: ${url}`,
    };

    export function optOutFooter(lang: string, unsubscribeUrl: string, senderIdentity: string): string {
      const build = FOOTER_COPY[lang] ?? FOOTER_COPY.English;
      return build(senderIdentity, unsubscribeUrl);
    }

    /** "Quem enviou" para o rodapé. RESEND_FROM é "Nome <email>" ou email cru. */
    export function senderIdentityFrom(resendFrom: string): string {
      const m = resendFrom.match(/^(.+?)\s*<.+>$/);
      return (m ? m[1] : resendFrom).trim();
    }
    ```

    3. `convex/lib/outreachAi.ts` — trocar `const LANG` (linha 5) por `export const LANG`. NENHUMA outra mudança neste arquivo (o prompt/fallback continuam iguais; a garantia de compliance vive no caminho do envio, não aqui).

    4. `tests/domain.test.ts` — adicionar os casos de normalizeEmail e hasWaOptIn ao import e ao corpo.

    5. `tests/compliance.test.ts` (NOVO): `import { optOutFooter, senderIdentityFrom } from "../convex/lib/compliance.ts";` + os casos do <behavior>.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function normalizeEmail" convex/lib/domain.ts`
    - `grep -q "export function hasWaOptIn" convex/lib/domain.ts`
    - `grep -q "export function optOutFooter" convex/lib/compliance.ts`
    - `grep -q "export function senderIdentityFrom" convex/lib/compliance.ts`
    - `grep -q "export const LANG" convex/lib/outreachAi.ts`
    - `grep -q "optOutFooter" tests/compliance.test.ts`
    - `pnpm test` exit 0 (todos os casos novos verdes; suíte existente segue passando)
  </acceptance_criteria>
  <done>normalizeEmail, hasWaOptIn, optOutFooter e senderIdentityFrom existem, LANG é exportado, e todos os casos novos em tests/domain.test.ts e tests/compliance.test.ts passam junto com a suíte existente.</done>
</task>

<task type="auto">
  <name>Task 3: Módulo suppressions.ts — addSuppression + add/isSuppressed/unsubscribeByToken</name>
  <files>convex/suppressions.ts</files>
  <read_first>
    - convex/leads.ts (ver internalQuery/internalMutation e o import de "./_generated/server")
    - convex/lib/domain.ts (normalizeEmail — recém-criado na Task 2)
    - .planning/phases/02-compliance-de-email-e-whatsapp/02-RESEARCH.md (§Pattern 2/3 — idempotência e dual-scope; §Anti-Patterns — no-leak)
  </read_first>
  <action>
    Criar `convex/suppressions.ts`. IMPORTANTE: mutations Convex NÃO podem chamar `ctx.runMutation`; por isso a lógica de insert-or-noop vive numa função pura `addSuppression(ctx, ...)` que tanto a `internalMutation add` quanto `outreach.suppress` (plano 02-02) quanto `unsubscribeByToken` chamam direto passando `ctx`.

    ```ts
    import { internalMutation, internalQuery } from "./_generated/server";
    import type { MutationCtx } from "./_generated/server";
    import type { Id } from "./_generated/dataModel";
    import { v } from "convex/values";
    import { normalizeEmail } from "./lib/domain";

    /** Insert idempotente: não duplica (email, orgId). email DEVE vir normalizado. */
    export async function addSuppression(
      ctx: MutationCtx,
      args: { email: string; orgId?: string; source: string; leadId?: Id<"leads"> },
    ): Promise<void> {
      const existing = args.orgId
        ? await ctx.db
            .query("suppressions")
            .withIndex("by_org_email", (q) => q.eq("orgId", args.orgId).eq("email", args.email))
            .first()
        : await ctx.db
            .query("suppressions")
            .withIndex("by_email", (q) => q.eq("email", args.email))
            .filter((q) => q.eq(q.field("orgId"), undefined))
            .first();
      if (existing) return; // idempotente
      await ctx.db.insert("suppressions", {
        email: args.email,
        orgId: args.orgId,
        source: args.source,
        leadId: args.leadId,
        at: Date.now(),
      });
    }

    export const add = internalMutation({
      args: {
        email: v.string(),
        orgId: v.optional(v.string()),
        source: v.string(),
        leadId: v.optional(v.id("leads")),
      },
      handler: async (ctx, args) => addSuppression(ctx, args),
    });

    /** Dual-scope: suprimido se houver row da própria org OU global (orgId undefined). */
    export const isSuppressed = internalQuery({
      args: { email: v.string(), orgId: v.string() },
      handler: async (ctx, { email, orgId }) => {
        const rows = await ctx.db
          .query("suppressions")
          .withIndex("by_email", (q) => q.eq("email", email))
          .collect();
        return rows.some((r) => r.orgId === orgId || r.orgId === undefined);
      },
    });

    /** Chamado pelo endpoint público (plano 02-03). No-leak: token desconhecido = no-op. */
    export const unsubscribeByToken = internalMutation({
      args: { token: v.string() },
      handler: async (ctx, { token }) => {
        const row = await ctx.db
          .query("outreach")
          .withIndex("by_unsub_token", (q) => q.eq("unsubscribeToken", token))
          .first();
        if (!row) return; // desconhecido/reusado → nada a fazer
        const lead = await ctx.db.get(row.leadId);
        if (!lead?.email) return;
        await addSuppression(ctx, {
          email: normalizeEmail(lead.email),
          orgId: row.orgId,
          source: "unsubscribe_link",
          leadId: row.leadId,
        });
      },
    });
    ```
  </action>
  <verify>
    <automated>npx convex codegen && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function addSuppression" convex/suppressions.ts`
    - `grep -q "export const isSuppressed" convex/suppressions.ts`
    - `grep -q "export const unsubscribeByToken" convex/suppressions.ts`
    - `grep -q "by_unsub_token" convex/suppressions.ts`
    - `grep -q "normalizeEmail" convex/suppressions.ts`
    - `pnpm typecheck` exit 0 (as funções aparecem em `internal.suppressions.*` no codegen)
  </acceptance_criteria>
  <done>convex/suppressions.ts exporta addSuppression + add/isSuppressed/unsubscribeByToken; isSuppressed casa org OU global; unsubscribeByToken faz no-op para token desconhecido; typecheck/codegen passam.</done>
</task>

</tasks>

<verification>
- `npx convex codegen && pnpm typecheck && pnpm lint && pnpm test` verde.
- `internal.suppressions.add`, `internal.suppressions.isSuppressed`, `internal.suppressions.unsubscribeByToken` existem no codegen (consumidos pelos planos de wave 2).
- Nenhum arquivo deste plano é tocado por 02-02/02-03/02-04 (só importado) — sem overlap de wave.
</verification>

<success_criteria>
- Tabela suppressions + índices; unsubscribeToken/by_unsub_token em outreach; waOptInAt/waOptInSource em leads; "wa_opt_in" em events.type.
- 4 helpers puros testados (normalizeEmail, hasWaOptIn, optOutFooter, senderIdentityFrom); LANG exportado.
- Módulo suppressions completo e idempotente; no-leak no unsubscribeByToken.
- `pnpm test` verde com os casos novos.
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-compliance-de-email-e-whatsapp/02-01-SUMMARY.md`.
</output>
