---
phase: 02-compliance-de-email-e-whatsapp
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - convex/http.ts
autonomous: true
requirements: [COMP-02]
nyquist_compliant: true

must_haves:
  truths:
    - "GET /unsubscribe?token=... grava a supressão e responde 200 com HTML de confirmação"
    - "POST /unsubscribe?token=... tem o mesmo efeito (RFC 8058 one-click)"
    - "Token desconhecido/reusado/ausente → mesma resposta 200 genérica (no-leak, sem PII)"
  artifacts:
    - path: "convex/http.ts"
      provides: "rotas GET+POST /unsubscribe no mesmo httpRouter do webhook Stripe"
      contains: "/unsubscribe"
  key_links:
    - from: "convex/http.ts:/unsubscribe"
      to: "internal.suppressions.unsubscribeByToken"
      via: "ctx.runMutation com o token do query param"
      pattern: "internal\\.suppressions\\.unsubscribeByToken"
---

<objective>
Expor o endpoint HTTP público de unsubscribe (COMP-02) no mesmo `httpRouter` que já hospeda o webhook do Stripe. Depende do plano 02-01 (índice `by_unsub_token` + `internal.suppressions.unsubscribeByToken`). Registra `GET` e `POST` no mesmo path `/unsubscribe` (RFC 8058 exige POST para o one-click), com um handler compartilhado que é burro e seguro: sem auth, idempotente, e SEMPRE a mesma resposta 200 genérica — nunca vaza se o token existe ou não.

Purpose: Transforma a promessa de opt-out num mecanismo público e forçado por código, compatível com as regras de bulk sender do Gmail/Yahoo (2024).
Output: convex/http.ts com rotas GET+POST /unsubscribe.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-compliance-de-email-e-whatsapp/02-CONTEXT.md
@.planning/phases/02-compliance-de-email-e-whatsapp/02-RESEARCH.md
@.planning/phases/02-compliance-de-email-e-whatsapp/02-01-SUMMARY.md

<interfaces>
<!-- Estado atual de convex/http.ts + o contrato do plano 02-01. Use direto. -->

`convex/http.ts` já tem: `import { httpRouter } from "convex/server";`, `import { httpAction } from "./_generated/server";`, `import { internal } from "./_generated/api";`, `const http = httpRouter();`, a rota `POST /stripe/webhook`, e `export default http;` no fim.

Do plano 02-01: `internal.suppressions.unsubscribeByToken({ token })` — internalMutation idempotente que faz lookup do outreach por `by_unsub_token`, resolve o email do lead, grava supressão org-scoped (source "unsubscribe_link"), e é no-op se o token não casar.

Convex httpRouter (02-RESEARCH §Pattern 1, verificado em router.d.ts): GET e POST no mesmo path exato são DUAS chamadas `http.route()` independentes — registrar o MESMO handler nas duas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rotas GET + POST /unsubscribe (handler compartilhado, no-leak)</name>
  <files>convex/http.ts</files>
  <read_first>
    - convex/http.ts (a rota existente do Stripe como molde; manter o webhook intacto; `export default http` fica no fim)
    - convex/suppressions.ts (unsubscribeByToken — do plano 02-01)
    - .planning/phases/02-compliance-de-email-e-whatsapp/02-RESEARCH.md (§Pattern 1; §Anti-Patterns "Leaking token validity")
  </read_first>
  <action>
    Em `convex/http.ts`, ANTES do `export default http;`, adicionar o handler compartilhado e as duas rotas. Reusar o MESMO objeto `httpAction` para GET e POST evita tipar o ctx manualmente e garante resposta idêntica:

    ```ts
    /**
     * Unsubscribe público (COMP-02). Sem auth, idempotente, no-leak.
     * GET  = clique no link do rodapé.  POST = RFC 8058 one-click.
     * Endpoint = `${CONVEX_SITE_URL}/unsubscribe?token=...`.
     */
    const unsubscribe = httpAction(async (ctx, req) => {
      const token = new URL(req.url).searchParams.get("token");
      if (token) {
        await ctx.runMutation(internal.suppressions.unsubscribeByToken, { token });
      }
      // SEMPRE a mesma resposta — token válido, inválido ou ausente. Nunca vaza existência.
      return new Response(
        `<!doctype html><html lang="en"><head><meta charset="utf-8">` +
          `<meta name="viewport" content="width=device-width, initial-scale=1">` +
          `<title>Unsubscribed</title></head>` +
          `<body style="font-family:system-ui,sans-serif;max-width:32rem;margin:4rem auto;padding:0 1.5rem;color:#0f172a">` +
          `<h1 style="font-size:1.5rem">You've been unsubscribed</h1>` +
          `<p style="color:#475569">You won't hear from us again.</p></body></html>`,
        { status: 200, headers: { "content-type": "text/html; charset=utf-8" } },
      );
    });

    http.route({ path: "/unsubscribe", method: "GET", handler: unsubscribe });
    http.route({ path: "/unsubscribe", method: "POST", handler: unsubscribe }); // RFC 8058 one-click
    ```

    NÃO tocar na rota `/stripe/webhook`. Não adicionar auth, não adicionar confirmação em duas etapas (a operação é idempotente e de baixa consequência — decisão travada no CONTEXT.md; ver 02-RESEARCH §Pitfall 7).
  </action>
  <verify>
    <automated>npx convex codegen && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "path: \"/unsubscribe\"" convex/http.ts` retorna 2 (GET e POST)
    - `grep -q 'method: "GET"' convex/http.ts && grep -q 'method: "POST", handler: unsubscribe' convex/http.ts`
    - `grep -q "internal.suppressions.unsubscribeByToken" convex/http.ts`
    - `grep -q "/stripe/webhook" convex/http.ts` (webhook Stripe intacto)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>GET e POST /unsubscribe registrados no mesmo router, ambos chamando unsubscribeByToken e retornando a mesma página 200; webhook Stripe intacto.</done>
</task>

</tasks>

<verification>
- `npx convex codegen && pnpm typecheck && pnpm lint && pnpm test` verde.
- Dashboard/`convex dev` lista `GET /unsubscribe` E `POST /unsubscribe`.
- Manual/smoke (pós-fase, requer deployment): `curl -i "$CONVEX_SITE_URL/unsubscribe?token=abc"` e `curl -i -X POST "$CONVEX_SITE_URL/unsubscribe?token=abc"` retornam 200 com o mesmo HTML; token válido grava em suppressions; token lixo também responde 200.
- Nenhum outro plano toca convex/http.ts (exclusivo desta wave).
</verification>

<success_criteria>
- GET e POST /unsubscribe funcionam e são idempotentes.
- Resposta 200 genérica para qualquer token (no-leak).
- Supressão gravada via internal.suppressions.unsubscribeByToken.
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-compliance-de-email-e-whatsapp/02-03-SUMMARY.md`.
</output>
