---
phase: 03-tracking-composer-e-localiza-o
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/preview-i18n.ts
  - tests/preview-i18n.test.ts
  - src/components/preview-site.tsx
autonomous: true
requirements: [L10N-01]
nyquist_compliant: true

must_haves:
  truths:
    - "O preview de um lead GB/IE renderiza em inglês, NL em holandês, SE em sueco e NO em norueguês"
    - "Nenhuma string em português (Venha, Tradição, Seg–Sáb) permanece em src/components/preview-site.tsx"
    - "countryCode desconhecido cai no fallback inglês"
    - "Os 4 locales têm exatamente as mesmas chaves (paridade)"
  artifacts:
    - path: "src/lib/preview-i18n.ts"
      provides: "dicionário puro de 4 locales (en/nl/sv/no) + localeForCountry(countryCode)"
      contains: "export function localeForCountry"
    - path: "tests/preview-i18n.test.ts"
      provides: "paridade de chaves + mapeamento countryCode→locale + grep-negativo de PT no componente"
      contains: "localeForCountry"
    - path: "src/components/preview-site.tsx"
      provides: "render localizado via DICTS[localeForCountry(content.countryCode)]"
      contains: "localeForCountry"
  key_links:
    - from: "src/components/preview-site.tsx"
      to: "src/lib/preview-i18n.ts"
      via: "const tr = DICTS[localeForCountry(content.countryCode)] no topo de PreviewSite"
      pattern: "localeForCountry\\(content.countryCode\\)"
---

<objective>
Fechar L10N-01: o preview público (`/p/[token]` e `/site/[slug]`) fala a língua do mercado do lead. Hoje `src/components/preview-site.tsx` tem 11 strings hardcoded em PT que aparecem para um dono de restaurante em Estocolmo. Este plano cria um dicionário TS puro (`src/lib/preview-i18n.ts`, ZERO import de Convex/React — dá pra testar com node:test) com 4 locales (`en`/`nl`/`sv`/`no`) e um mapa `countryCode → locale` (GB/IE→en, NL→nl, SE→sv, NO→no, fallback en), e migra TODAS as strings PT do componente para `DICTS[localeForCountry(content.countryCode)]`. Layout intocado — só as strings. `content.countryCode` já chega do backend (convex/previews.ts:53/90/159), sem mudança de schema.

Purpose: Zero português em qualquer página pública; o produto fala a língua do prospect, coerente com o posicionamento compliance-por-mercado.
Output: dicionário puro testado + componente localizado.

NOTA DE PARALELISMO (wave 1): roda em paralelo com 03-01 e 03-03. Commitar com pathspec EXPLÍCITO (`git commit -- src/lib/preview-i18n.ts tests/preview-i18n.test.ts src/components/preview-site.tsx`) — nunca `git add -A`/`-a`. Os arquivos deste plano são disjuntos dos outros dois planos da wave.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tracking-composer-e-localiza-o/03-CONTEXT.md
@.planning/phases/03-tracking-composer-e-localiza-o/03-RESEARCH.md

<interfaces>
<!-- Contratos exatos. PreviewContent já expõe countryCode (src/components/preview-site.tsx:3-11). -->

```ts
export interface PreviewContent {
  name: string; category: string | null; city: string | null;
  phone: string | null; rating: number | null; reviewsCount: number | null;
  countryCode: string; // ISO alpha-2, ex.: "GB", "SE"
}
```

Precedente de dicionário por idioma (`convex/lib/compliance.ts` — Fase 2): `Record<Chave, valor>` puro, testado em `tests/compliance.test.ts`. Este módulo segue o MESMO estilo, mas vive em `src/lib/` (consumido por componente React) e tem mais chaves.

Padrão de teste (`tests/compliance.test.ts`): `import { test } from "node:test"; import assert from "node:assert/strict";` + import relativo com extensão `.ts`. O glob do `package.json` é `tests/*.test.ts`; imports cruzam diretório (`tests/domain.test.ts` importa `../convex/lib/domain.ts`), então `../src/lib/preview-i18n.ts` funciona.

Import alias do componente: usar `@/lib/preview-i18n` (mesmo estilo de `@/lib/playbook` em lead-detail.tsx).

NÃO reusar o `LANG` de `convex/lib/outreachAi.ts` (valores são "English"/"Swedish", não códigos de locale) — construir um mapa independente `countryCode → "en"|"nl"|"sv"|"no"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dicionário puro preview-i18n + testes (paridade + mapeamento)</name>
  <files>src/lib/preview-i18n.ts, tests/preview-i18n.test.ts</files>
  <read_first>
    - convex/lib/compliance.ts (shape de dicionário por idioma; precedente de módulo puro)
    - tests/compliance.test.ts (padrão node:test/assert strict, import .ts)
    - .planning/phases/03-tracking-composer-e-localiza-o/03-RESEARCH.md (§Code Examples — inventário das 11 strings PT com números de linha; §Open Questions 3 — convenção 24h para nl/sv/no vs 12h en)
  </read_first>
  <behavior>
    Escrever os testes PRIMEIRO (RED), depois o dicionário (GREEN).

    localeForCountry(countryCode: string): Locale
    - localeForCountry("GB") === "en"
    - localeForCountry("IE") === "en"
    - localeForCountry("NL") === "nl"
    - localeForCountry("SE") === "sv"
    - localeForCountry("NO") === "no"
    - localeForCountry("DE") === "en"  // fallback
    - localeForCountry("gb") === "en"  // case-insensitive (usa toUpperCase)

    Paridade de chaves:
    - Object.keys(DICTS.en).sort() === Object.keys(DICTS.nl).sort() === (sv) === (no)

    Funções do dicionário respondem (smoke):
    - DICTS.sv.featureLocationBodyWithCity("Stockholm") inclui "Stockholm"
    - DICTS.en.visitBody({ name: "Joe", category: "café", city: "London" }) inclui "Joe" e "London"
  </behavior>
  <action>
    1. Criar `src/lib/preview-i18n.ts` (puro, SEM import de Convex/React):

    ```ts
    export type Locale = "en" | "nl" | "sv" | "no";

    const LOCALE_BY_COUNTRY: Record<string, Locale> = {
      GB: "en", IE: "en", NL: "nl", SE: "sv", NO: "no",
    };

    export function localeForCountry(countryCode: string): Locale {
      return LOCALE_BY_COUNTRY[countryCode.toUpperCase()] ?? "en";
    }

    export interface PreviewDict {
      call: string;
      callNow: string;
      whatsapp: string;
      reviews: string;
      heroSubtitle: string;
      featureQualityTitle: string;
      featureQualityBody: string;
      featureServiceTitle: string;
      featureServiceBody: string;
      featureLocationTitle: string;
      featureLocationBodyWithCity: (city: string) => string;
      featureLocationBodyNoCity: string;
      visitHeading: string;
      visitBody: (o: { name: string; category: string | null; city: string | null }) => string;
      phoneLabel: string;
      whereLabel: string;
      hoursLabel: string;
      hoursValue: string;
    }

    const en: PreviewDict = {
      call: "Call",
      callNow: "Call now",
      whatsapp: "WhatsApp",
      reviews: "reviews",
      heroSubtitle:
        "Craft, a warm welcome, and the trust of people who already know us. Book, call, or drop by.",
      featureQualityTitle: "Quality",
      featureQualityBody: "Made with care, from start to finish.",
      featureServiceTitle: "Service",
      featureServiceBody: "Close to you, just the way you like it.",
      featureLocationTitle: "In the heart of the city",
      featureLocationBodyWithCity: (city) => `Right in the centre of ${city}.`,
      featureLocationBodyNoCity: "Easy to reach.",
      visitHeading: "Come and visit us",
      visitBody: ({ name, category, city }) =>
        `${name} is a name to know${category ? ` in ${category}` : " in the neighbourhood"}${city ? `, in ${city}` : ""}. We're ready to welcome you.`,
      phoneLabel: "Phone",
      whereLabel: "Where",
      hoursLabel: "Hours",
      hoursValue: "Mon–Sat · 9am–7pm",
    };

    const nl: PreviewDict = {
      call: "Bellen",
      callNow: "Nu bellen",
      whatsapp: "WhatsApp",
      reviews: "beoordelingen",
      heroSubtitle:
        "Vakmanschap, een warm onthaal en het vertrouwen van wie ons al kent. Reserveer, bel of loop even binnen.",
      featureQualityTitle: "Kwaliteit",
      featureQualityBody: "Met zorg gemaakt, van begin tot eind.",
      featureServiceTitle: "Service",
      featureServiceBody: "Dichtbij, precies zoals u het wilt.",
      featureLocationTitle: "In het hart van de stad",
      featureLocationBodyWithCity: (city) => `Midden in het centrum van ${city}.`,
      featureLocationBodyNoCity: "Makkelijk te bereiken.",
      visitHeading: "Kom langs",
      visitBody: ({ name, category, city }) =>
        `${name} is een begrip${category ? ` in ${category}` : " in de buurt"}${city ? `, in ${city}` : ""}. We heten u graag welkom.`,
      phoneLabel: "Telefoon",
      whereLabel: "Waar",
      hoursLabel: "Openingstijden",
      hoursValue: "Ma–Za · 9.00–19.00",
    };

    const sv: PreviewDict = {
      call: "Ring",
      callNow: "Ring nu",
      whatsapp: "WhatsApp",
      reviews: "omdömen",
      heroSubtitle:
        "Hantverk, ett varmt bemötande och tryggheten hos dem som redan känner oss. Boka, ring eller kom förbi.",
      featureQualityTitle: "Kvalitet",
      featureQualityBody: "Gjort med omsorg, från början till slut.",
      featureServiceTitle: "Service",
      featureServiceBody: "Nära dig, precis som du vill ha det.",
      featureLocationTitle: "Mitt i stan",
      featureLocationBodyWithCity: (city) => `Mitt i centrala ${city}.`,
      featureLocationBodyNoCity: "Lätt att hitta.",
      visitHeading: "Kom och besök oss",
      visitBody: ({ name, category, city }) =>
        `${name} är ett namn att känna till${category ? ` inom ${category}` : " i området"}${city ? `, i ${city}` : ""}. Vi tar gärna emot dig.`,
      phoneLabel: "Telefon",
      whereLabel: "Var",
      hoursLabel: "Öppettider",
      hoursValue: "Mån–Lör · 09.00–19.00",
    };

    const no: PreviewDict = {
      call: "Ring",
      callNow: "Ring nå",
      whatsapp: "WhatsApp",
      reviews: "anmeldelser",
      heroSubtitle:
        "Håndverk, en varm velkomst og tryggheten fra dem som allerede kjenner oss. Bestill, ring eller stikk innom.",
      featureQualityTitle: "Kvalitet",
      featureQualityBody: "Laget med omtanke, fra start til slutt.",
      featureServiceTitle: "Service",
      featureServiceBody: "Nær deg, slik du liker det.",
      featureLocationTitle: "Midt i sentrum",
      featureLocationBodyWithCity: (city) => `Midt i sentrum av ${city}.`,
      featureLocationBodyNoCity: "Lett å finne.",
      visitHeading: "Kom og besøk oss",
      visitBody: ({ name, category, city }) =>
        `${name} er et navn å kjenne til${category ? ` innen ${category}` : " i nabolaget"}${city ? `, i ${city}` : ""}. Vi tar gjerne imot deg.`,
      phoneLabel: "Telefon",
      whereLabel: "Hvor",
      hoursLabel: "Åpningstider",
      hoursValue: "Man–Lør · 09.00–19.00",
    };

    export const DICTS: Record<Locale, PreviewDict> = { en, nl, sv, no };
    ```

    2. Criar `tests/preview-i18n.test.ts` com os casos do <behavior>:
    ```ts
    import { test } from "node:test";
    import assert from "node:assert/strict";
    import { DICTS, localeForCountry } from "../src/lib/preview-i18n.ts";

    test("preview-i18n: countryCode mapeia para o locale correto", () => {
      assert.equal(localeForCountry("GB"), "en");
      assert.equal(localeForCountry("IE"), "en");
      assert.equal(localeForCountry("NL"), "nl");
      assert.equal(localeForCountry("SE"), "sv");
      assert.equal(localeForCountry("NO"), "no");
      assert.equal(localeForCountry("DE"), "en"); // fallback
      assert.equal(localeForCountry("gb"), "en"); // case-insensitive
    });

    test("preview-i18n: todos os locales têm as mesmas chaves", () => {
      const keys = Object.keys(DICTS.en).sort();
      for (const l of ["nl", "sv", "no"] as const) {
        assert.deepEqual(Object.keys(DICTS[l]).sort(), keys);
      }
    });

    test("preview-i18n: funções interpoladas incluem os argumentos", () => {
      assert.ok(DICTS.sv.featureLocationBodyWithCity("Stockholm").includes("Stockholm"));
      const body = DICTS.en.visitBody({ name: "Joe", category: "café", city: "London" });
      assert.ok(body.includes("Joe") && body.includes("London"));
    });
    ```
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function localeForCountry" src/lib/preview-i18n.ts`
    - `grep -q "export const DICTS" src/lib/preview-i18n.ts`
    - `grep -q 'GB: "en"' src/lib/preview-i18n.ts && grep -q 'SE: "sv"' src/lib/preview-i18n.ts && grep -q 'NO: "no"' src/lib/preview-i18n.ts && grep -q 'NL: "nl"' src/lib/preview-i18n.ts`
    - `pnpm test` exit 0 (paridade + mapeamento verdes; suíte existente segue passando)
  </acceptance_criteria>
  <done>preview-i18n.ts exporta Locale, localeForCountry (com fallback en e case-insensitive) e DICTS com 4 locales de chaves idênticas; os 3 testes novos passam junto com a suíte existente.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar preview-site.tsx para o dicionário + teste grep-negativo de PT</name>
  <files>src/components/preview-site.tsx, tests/preview-i18n.test.ts</files>
  <read_first>
    - src/components/preview-site.tsx (as 11 strings PT nas linhas do inventário; layout/classes que NÃO mudam)
    - src/lib/preview-i18n.ts (o dicionário criado na Task 1 — DICTS, localeForCountry, PreviewDict)
    - .planning/phases/03-tracking-composer-e-localiza-o/03-RESEARCH.md (§Pitfall 5 — migração parcial deixa string PT solta em ternário/template)
  </read_first>
  <action>
    Editar `src/components/preview-site.tsx`. Layout, classes e estrutura JSX permanecem idênticos — só as strings PT viram lookups.

    1. Adicionar import no topo: `import { DICTS, localeForCountry } from "@/lib/preview-i18n";`

    2. Incluir `countryCode` na desestruturação e derivar o dicionário no início de `PreviewSite`:
    ```ts
    const { name, category, city, phone, rating, reviewsCount, countryCode } = content;
    const cat = category ? category.replace(/_/g, " ") : null;
    const tr = DICTS[localeForCountry(countryCode)];
    ```
    (Nome `tr` para não colidir com a chave `t` dos itens de highlight.)

    3. Substituir cada string PT (o texto à direita → o lookup):
       - Header CTA "Ligar" → `{tr.call}`
       - Hero `<p>` "Tradição, atendimento próximo... para conhecer." → `{tr.heroSubtitle}`
       - Hero CTA "Ligar agora" → `{tr.callNow}`
       - Hero CTA "WhatsApp" → `{tr.whatsapp}`
       - Sufixo de reviews `· {reviewsCount} avaliações` → `· {reviewsCount} {tr.reviews}`
       - Array de highlights:
         - `{ t: "Qualidade", d: "Feito com cuidado, do começo ao fim." }` → `{ t: tr.featureQualityTitle, d: tr.featureQualityBody }`
         - `{ t: "Atendimento", d: "Perto de você, do jeito que gosta." }` → `{ t: tr.featureServiceTitle, d: tr.featureServiceBody }`
         - `{ t: "No coração da cidade", d: city ? `Bem no centro de ${city}.` : "Fácil de chegar." }` → `{ t: tr.featureLocationTitle, d: city ? tr.featureLocationBodyWithCity(city) : tr.featureLocationBodyNoCity }`
       - Contact H2 "Venha nos visitar" → `{tr.visitHeading}`
       - Contact `<p>` inteiro (`{name} é referência {cat ? ...}{city ? ...}. Estamos prontos para te receber.`) → `{tr.visitBody({ name, category: cat, city })}`
       - `<dt>` "Telefone" → `{tr.phoneLabel}`
       - `<dt>` "Onde" → `{tr.whereLabel}`
       - `<dt>` "Horário" → `{tr.hoursLabel}`
       - `<dd>` "Seg–Sáb · 9h–19h" → `{tr.hoursValue}`

    O eyebrow de categoria (`{cat}{city ? ` · ${city}` : ""}`) e o nome no header/footer são DADOS do negócio, não copy — NÃO traduzir. "WhatsApp" continua "WhatsApp" (nome próprio) via `tr.whatsapp`.

    4. Adicionar ao `tests/preview-i18n.test.ts` o caso grep-negativo (lê o arquivo do componente e garante zero strings PT):
    ```ts
    import { readFileSync } from "node:fs";

    test("preview-site: nenhuma string PT hardcoded permanece", () => {
      const src = readFileSync(new URL("../src/components/preview-site.tsx", import.meta.url), "utf8");
      assert.equal(src.match(/Venha|Tradição|Seg–Sáb/), null);
    });
    ```
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -nE "Venha|Tradição|Seg–Sáb" src/components/preview-site.tsx` (retorna vazio, exit 1 do grep = sem match)
    - `! grep -nE "avaliações|Qualidade|Atendimento|Telefone" src/components/preview-site.tsx` (as strings PT do inventário sumiram)
    - `grep -q "localeForCountry(countryCode)" src/components/preview-site.tsx`
    - `grep -q "tr.visitBody" src/components/preview-site.tsx`
    - `pnpm test` exit 0 (inclui o novo caso grep-negativo verde)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>preview-site.tsx deriva o locale de content.countryCode e usa DICTS para todas as 11 strings; nenhuma string PT do inventário permanece; o teste grep-negativo e a suíte passam; typecheck verde.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm lint && pnpm test` verde.
- `/site/[slug]` herda a localização de graça (usa o mesmo PreviewSite) — sem trabalho extra.
- Arquivos disjuntos de 03-01 e 03-03 — sem overlap de wave.
</verification>

<success_criteria>
- Dicionário puro de 4 locales com paridade de chaves e fallback en, testado.
- preview-site.tsx sem nenhuma string PT; render no idioma do mercado do lead.
- Suíte verde.
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-tracking-composer-e-localiza-o/03-02-SUMMARY.md`.
</output>
