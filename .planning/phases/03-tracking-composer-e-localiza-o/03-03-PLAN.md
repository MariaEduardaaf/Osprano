---
phase: 03-tracking-composer-e-localiza-o
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/outreach.ts
autonomous: true
requirements: [TRCK-02, OUTR-01]
nyquist_compliant: true

must_haves:
  truths:
    - "A tabela outreach ganha o campo opcional repliedAt (v.optional(v.number()))"
    - "markReplied grava status replied + repliedAt e insere um evento type reply, com ownership check pt-BR"
    - "updateDraft persiste subject/body no row de outreach existente (erro pt-BR se não há rascunho), SEM tocar o rodapé de opt-out"
    - "A query outbox usa repliedAt como primeiro termo de activityAt e expõe repliedAt no item"
  artifacts:
    - path: "convex/schema.ts"
      provides: "campo repliedAt em outreach"
      contains: "repliedAt: v.optional(v.number())"
    - path: "convex/outreach.ts"
      provides: "mutations markReplied e updateDraft + fix de activityAt na outbox"
      contains: "export const markReplied"
  key_links:
    - from: "convex/outreach.ts:markReplied"
      to: "events (type reply)"
      via: "ctx.db.insert events com type reply após patch de status/repliedAt"
      pattern: "type: \"reply\""
    - from: "convex/outreach.ts:outbox"
      to: "row.repliedAt"
      via: "activityAt prioriza repliedAt sobre openedAt/sentAt"
      pattern: "row.repliedAt \\?\\?"
---

<objective>
Fundação backend de TRCK-02 e OUTR-01 num só plano, porque ambos tocam `convex/schema.ts` e `convex/outreach.ts` (blocker já sinalizado no STATE.md). Adiciona: (1) o campo aditivo `repliedAt` em `outreach`; (2) a mutation `markReplied({ leadId })` — status replied + repliedAt real + evento reply, seguindo o shape ownership-check→patch→insert já usado 4x no repo; (3) a mutation `updateDraft({ leadId, subject, body })` — persiste as edições do composer sem duplicar a lógica de rodapé de opt-out (o `send` da Fase 2 já re-garante isso idempotentemente); (4) o fix da outbox: `activityAt` passa a priorizar `repliedAt`, senão itens marcados "respondeu" continuariam exibindo o horário de abertura/envio (pitfall achado na pesquisa, não citado no CONTEXT).

A UI que chama essas mutations vem no plano 03-04 (wave 2). Este plano é backend puro — mantém `convex/outreach.ts` fora do alcance de qualquer plano da wave 1.

Purpose: O funil prometido (rascunho→enviado→abriu→respondeu) fica real end-to-end, e "o que se vê no composer é o que sai".
Output: schema + 2 mutations + fix da outbox.

NOTA DE PARALELISMO (wave 1): roda em paralelo com 03-01 (previews.ts) e 03-02 (preview-site/i18n) — arquivos disjuntos. Commitar com pathspec EXPLÍCITO (`git commit -- convex/schema.ts convex/outreach.ts`) — nunca `git add -A`/`-a`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tracking-composer-e-localiza-o/03-CONTEXT.md
@.planning/phases/03-tracking-composer-e-localiza-o/03-RESEARCH.md

<interfaces>
<!-- Contratos exatos do código atual. Use direto, sem explorar. -->

Tabela outreach (`convex/schema.ts:130-150`) — já tem status com literal "replied" e index by_lead:
```ts
outreach: defineTable({
  orgId, leadId, channel, subject: v.optional(v.string()), body: v.optional(v.string()),
  status: v.union(v.literal("draft"), v.literal("sent"), v.literal("opened"), v.literal("replied"), v.literal("bounced")),
  previewToken, unsubscribeToken, sentAt: v.optional(v.number()), openedAt: v.optional(v.number()),
}).index("by_org",...).index("by_lead",...).index("by_unsub_token",...)
```
events.type (`convex/schema.ts:154-160`) JÁ inclui `v.literal("reply")` — nenhuma mudança na union de events.

Shape de referência — `markSent` (`convex/outreach.ts:172-189`), o padrão ownership-check→query row by_lead→patch→insert events a replicar (mas markSent faz no-op silencioso se não há row; markReplied/updateDraft devem LANÇAR erro pt-BR — as duas UIs só renderizam o botão quando já existe row, então um erro só dispararia num bug real):
```ts
export const markSent = mutation({
  args: { leadId: v.id("leads") },
  handler: async (ctx, { leadId }) => {
    const orgId = await requireOrgId(ctx);
    const lead = await ctx.db.get(leadId);
    if (!lead || lead.orgId !== orgId) throw new Error("Lead não encontrado");
    const now = Date.now();
    const row = await ctx.db.query("outreach").withIndex("by_lead", (q) => q.eq("leadId", leadId)).first();
    if (row) await ctx.db.patch(row._id, { status: "sent", sentAt: now });
    if (lead.stage === "base") await ctx.db.patch(leadId, { stage: "approached", stageUpdatedAt: now });
    await ctx.db.insert("events", { orgId, type: "email_sent", leadId, at: now });
  },
});
```
outbox activityAt atual (`convex/outreach.ts:68`): `activityAt: row.openedAt ?? lastOpenedAt ?? row.sentAt ?? row._creationTime,` — SEM termo de repliedAt.

`send` (`convex/outreach.ts:210-269`) lê `row.subject`/`row.body` fresco via `api.outreach.getForLead` no momento do envio e re-garante o rodapé idempotentemente → escrever em subject/body via updateDraft basta, `send` NÃO muda. `upsertDraft` (linhas 90-127) é onde vive `withOptOutFooter` — NÃO duplicar em updateDraft.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: repliedAt no schema + markReplied + fix de activityAt na outbox (TRCK-02)</name>
  <files>convex/schema.ts, convex/outreach.ts</files>
  <read_first>
    - convex/schema.ts (tabela outreach linhas 130-150 — onde adicionar repliedAt; confirmar que status já tem "replied" e events.type já tem "reply")
    - convex/outreach.ts (markSent linhas 172-189 como shape a copiar; outbox linhas 25-75, especialmente a linha 68 de activityAt e o objeto empurrado em items.push)
    - .planning/phases/03-tracking-composer-e-localiza-o/03-RESEARCH.md (§Pattern 2 e §Pitfall 1 — outbox activityAt; §Pitfall 3 — guard de row inexistente; §Open Questions 1 — lançar erro em vez de no-op)
  </read_first>
  <action>
    1. `convex/schema.ts` — na tabela `outreach`, logo após `openedAt: v.optional(v.number()),` (linha 146), adicionar:
    ```ts
    repliedAt: v.optional(v.number()), // TRCK-02: timestamp real da marcação manual de "respondeu"
    ```
    Não mexer em status nem em events.type (já têm "replied"/"reply"). Rodar `npx convex codegen` após salvar (adicionar campo NÃO faz backfill; rows antigas retornam undefined — esperado).

    2. `convex/outreach.ts` — adicionar a mutation `markReplied` logo após `markSent` (após a linha 189):
    ```ts
    /** TRCK-02: marca uma abordagem como "respondeu" manualmente (outbox / lead-detail). */
    export const markReplied = mutation({
      args: { leadId: v.id("leads") },
      handler: async (ctx, { leadId }) => {
        const orgId = await requireOrgId(ctx);
        const lead = await ctx.db.get(leadId);
        if (!lead || lead.orgId !== orgId) throw new Error("Lead não encontrado");
        const row = await ctx.db
          .query("outreach")
          .withIndex("by_lead", (q) => q.eq("leadId", leadId))
          .first();
        if (!row) throw new Error("Nenhuma abordagem encontrada para este lead.");
        const now = Date.now();
        await ctx.db.patch(row._id, { status: "replied", repliedAt: now });
        await ctx.db.insert("events", { orgId, type: "reply", leadId, at: now });
        return { repliedAt: now };
      },
    });
    ```
    NÃO mexer no estágio do Kanban (replied é status do outreach, não do funil — o usuário controla o funil).

    3. `convex/outreach.ts` — corrigir a `outbox`:
       - Linha 68, trocar por (repliedAt na frente da cadeia):
       ```ts
       activityAt: row.repliedAt ?? row.openedAt ?? lastOpenedAt ?? row.sentAt ?? row._creationTime,
       ```
       - No mesmo objeto `items.push({...})`, adicionar o campo (para a UI/row action poder ler direto):
       ```ts
       repliedAt: row.repliedAt ?? null,
       ```
  </action>
  <verify>
    <automated>npx convex codegen && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "repliedAt: v.optional(v.number())" convex/schema.ts`
    - `grep -q "export const markReplied" convex/outreach.ts`
    - `grep -q 'status: "replied", repliedAt: now' convex/outreach.ts`
    - `grep -q 'type: "reply"' convex/outreach.ts`
    - `grep -q "row.repliedAt ?? row.openedAt" convex/outreach.ts` (activityAt corrigido)
    - `grep -q "Nenhuma abordagem encontrada" convex/outreach.ts` (guard pt-BR de row inexistente)
    - `pnpm typecheck` exit 0 (`api.outreach.markReplied` existe no codegen)
  </acceptance_criteria>
  <done>outreach tem repliedAt; markReplied grava status/repliedAt + evento reply com ownership check e erro pt-BR se não há row; outbox prioriza repliedAt em activityAt e expõe repliedAt; codegen/typecheck passam.</done>
</task>

<task type="auto">
  <name>Task 2: updateDraft — persiste subject/body sem duplicar o rodapé (OUTR-01)</name>
  <files>convex/outreach.ts</files>
  <read_first>
    - convex/outreach.ts (upsertDraft linhas 90-127 — onde vive withOptOutFooter, que NÃO deve ser duplicado; send linhas 210-269 — confirma que o rodapé é re-garantido no envio)
    - .planning/phases/03-tracking-composer-e-localiza-o/03-CONTEXT.md (§OUTR-01 — erro pt-BR se não existe draft; não duplicar rodapé)
    - .planning/phases/03-tracking-composer-e-localiza-o/03-RESEARCH.md (§Pattern 3; §Anti-Patterns — não duplicar o footer em updateDraft)
  </read_first>
  <action>
    `convex/outreach.ts` — adicionar a mutation `updateDraft` (perto de markReplied / após markSent):
    ```ts
    /** OUTR-01: persiste o que o usuário editou no composer, antes de send/markSent/copy.
        NÃO injeta rodapé de opt-out — o send (Fase 2) re-garante footer/headers de forma
        idempotente, então uma edição do usuário nunca remove a garantia de compliance. */
    export const updateDraft = mutation({
      args: { leadId: v.id("leads"), subject: v.string(), body: v.string() },
      handler: async (ctx, { leadId, subject, body }) => {
        const orgId = await requireOrgId(ctx);
        const lead = await ctx.db.get(leadId);
        if (!lead || lead.orgId !== orgId) throw new Error("Lead não encontrado");
        const row = await ctx.db
          .query("outreach")
          .withIndex("by_lead", (q) => q.eq("leadId", leadId))
          .first();
        if (!row) throw new Error("Nenhum rascunho encontrado para este lead.");
        await ctx.db.patch(row._id, { subject, body });
      },
    });
    ```
    NÃO chamar `withOptOutFooter` aqui (é trabalho do upsertDraft e do send). NÃO mudar `status` (permanece o que estava — ex.: draft/sent). NÃO alterar `send`, `getForLead` nem `upsertDraft`.
  </action>
  <verify>
    <automated>npx convex codegen && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export const updateDraft" convex/outreach.ts`
    - `grep -q "Nenhum rascunho encontrado" convex/outreach.ts` (erro pt-BR de row inexistente)
    - Dentro do corpo de updateDraft NÃO há chamada a withOptOutFooter (a action instrui explicitamente a não duplicar; conferir no diff que updateDraft só faz `ctx.db.patch(row._id, { subject, body })`)
    - `pnpm typecheck` exit 0 (`api.outreach.updateDraft` existe no codegen)
  </acceptance_criteria>
  <done>updateDraft faz ownership check, lança erro pt-BR se não há row, e faz patch apenas de subject/body sem tocar no rodapé; codegen/typecheck passam.</done>
</task>

</tasks>

<verification>
- `npx convex codegen && pnpm typecheck && pnpm lint && pnpm test` verde (suíte pura existente segue passando; estas mutations Convex não têm harness unit — verificação de runtime é manual/smoke via UI do plano 03-04, ver 03-VALIDATION.md).
- `api.outreach.markReplied` e `api.outreach.updateDraft` existem no codegen (consumidos pelo plano 03-04 na wave 2).
- Arquivos disjuntos de 03-01 e 03-02 — sem overlap de wave.
</verification>

<success_criteria>
- outreach.repliedAt no schema; markReplied e updateDraft registradas; outbox activityAt prioriza repliedAt.
- Nenhuma duplicação de lógica de rodapé; send/upsertDraft intocados.
- codegen/typecheck/lint/test verdes.
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-tracking-composer-e-localiza-o/03-03-SUMMARY.md`.
</output>
