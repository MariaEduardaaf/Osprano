---
phase: 03-tracking-composer-e-localiza-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/previews.ts
autonomous: true
requirements: [TRCK-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Vendedor autenticado (mesma sessão Clerk do workspace) abrindo o próprio link de preview NÃO incrementa openCount, NÃO cria evento preview_open e NÃO move o lead de base para approached"
    - "Prospect real (sem sessão Clerk, identity null) abrindo o mesmo link conta normalmente — openCount incrementa e o estágio avança"
    - "Modo demo (sem ClerkProvider → identity sempre null) continua contando toda abertura"
  artifacts:
    - path: "convex/previews.ts"
      provides: "guarda de self-open no recordOpen, antes de qualquer write"
      contains: "identity.subject === preview.orgId"
  key_links:
    - from: "convex/previews.ts:recordOpen"
      to: "ctx.auth.getUserIdentity()"
      via: "early-return quando o caller é o dono do workspace, antes do patch/insert/stage"
      pattern: "getUserIdentity"
---

<objective>
Fechar TRCK-01: a abertura do preview pelo próprio vendedor (logado no app, mesmo browser) deixa de corromper o sinal de compra. A rota pública `/p/[token]` roda dentro do mesmo `ConvexClientProvider` + `ClerkProvider` do root layout, então quando o dono do workspace abre o próprio link o token do Clerk acompanha a mutation `recordOpen` e `ctx.auth.getUserIdentity()` resolve a identidade dele. Um prospect real nunca tem sessão Clerk → `identity` é null → conta normalmente. Fix cirúrgico: uma guarda de early-return de 3 linhas no topo do handler de `recordOpen`, antes de qualquer escrita. NÃO tocar `PreviewTracker` (travado pelo CONTEXT). NÃO adicionar query param nem checagem no cliente.

Purpose: O funil (`base → approached`, "abriu" na outbox) reflete só comportamento genuíno de prospect.
Output: `recordOpen` com guarda de self-open baseada em identidade.

NOTA DE PARALELISMO (wave 1): este plano roda em paralelo com 03-02 e 03-03. Ao commitar, usar pathspec EXPLÍCITO (`git commit -- convex/previews.ts ...`) — nunca `git add -A`/`git commit -a` — para evitar a corrida de working tree observada na Fase 2. Este plano toca UM só arquivo.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tracking-composer-e-localiza-o/03-CONTEXT.md
@.planning/phases/03-tracking-composer-e-localiza-o/03-RESEARCH.md

<interfaces>
<!-- Estado atual exato de recordOpen (convex/previews.ts:115-142). Use direto. -->

```ts
export const recordOpen = mutation({
  args: { token: v.string() },
  handler: async (ctx, { token }) => {
    const preview = await ctx.db
      .query("previews")
      .withIndex("by_token", (q) => q.eq("token", token))
      .first();
    if (!preview) return;

    const now = Date.now();
    await ctx.db.patch(preview._id, { openCount: preview.openCount + 1, lastOpenedAt: now });
    await ctx.db.insert("events", { orgId: preview.orgId, type: "preview_open", leadId: preview.leadId, previewToken: token, at: now });
    const lead = await ctx.db.get(preview.leadId);
    if (lead && lead.stage === "base") {
      await ctx.db.patch(lead._id, { stage: "approached", stageUpdatedAt: now });
    }
  },
});
```

Modelo de tenant (`convex/model/tenant.ts`): `orgId === identity.subject` (Clerk user subject; sem suporte a Clerk-Org ainda). Logo `identity.subject === preview.orgId` é a checagem correta de "dono abrindo o próprio preview". `preview.orgId` é gravado a partir do mesmo `requireOrgId`/`lead.orgId` na geração (convex/previews.ts:66,93).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guarda de self-open baseada em identidade no recordOpen</name>
  <files>convex/previews.ts</files>
  <read_first>
    - convex/previews.ts (recordOpen, linhas 115-142 — a mutation pública que este plano altera; e generate/ensureForLead para confirmar que preview.orgId vem de lead.orgId)
    - convex/model/tenant.ts (requireOrgId retorna identity.subject — confirma que orgId É o subject do Clerk neste MVP)
    - .planning/phases/03-tracking-composer-e-localiza-o/03-RESEARCH.md (§Pattern 1 e §Pitfall 4 — a guarda é suficiente no regime estável; a janela de corrida do PreviewTracker fica documentada só para QA manual, NÃO é código desta fase)
    - .planning/phases/03-tracking-composer-e-localiza-o/03-CONTEXT.md (§TRCK-01 — PreviewTracker NÃO muda; limitação da aba anônima é aceita e comentada)
  </read_first>
  <action>
    Editar `convex/previews.ts`, handler de `recordOpen`. Logo APÓS `if (!preview) return;` (linha 122) e ANTES de `const now = Date.now();` (linha 124), inserir a guarda:

    ```ts
    // TRCK-01: o dono do workspace abrindo o próprio preview (logado no app, no
    // mesmo browser) NÃO pode contar como sinal de prospect. Um prospect real
    // nunca tem sessão Clerk, então identity é null para ele. Limitação aceita e
    // documentada: uma aba anônima/privada do próprio dono conta como prospect
    // (igual a qualquer ferramenta de tracking). Modo demo não tem ClerkProvider
    // → getUserIdentity() sempre null → aberturas de demo seguem contando (desejado).
    const identity = await ctx.auth.getUserIdentity();
    if (identity && identity.subject === preview.orgId) return;
    ```

    NÃO alterar mais nada: o `patch`/`insert("events")`/mudança de estágio permanecem exatamente como estão, só passam a ser inalcançáveis quando o caller é o dono. NÃO tocar `src/components/preview-tracker.tsx`. NÃO adicionar parâmetro `?self=1` nem checagem `useUser()` no cliente (isso quebraria a contagem legítima do modo demo). Como não há mudança de schema nem nova função registrada, `pnpm typecheck` já cobre; não é preciso codegen.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "getUserIdentity" convex/previews.ts` (guarda presente)
    - `grep -q "identity.subject === preview.orgId" convex/previews.ts`
    - A guarda aparece ANTES da primeira escrita: `grep -n "identity.subject === preview.orgId\|ctx.db.patch(preview._id" convex/previews.ts` mostra a linha do `identity` com número MENOR que a do primeiro `patch`
    - `git diff --exit-code src/components/preview-tracker.tsx` (PreviewTracker intocado — sem alterações)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>recordOpen retorna cedo, sem incrementar openCount, sem inserir evento e sem mover estágio, quando há identidade autenticada cujo subject == preview.orgId; caller anônimo (identity null) segue o caminho antigo; PreviewTracker inalterado; typecheck verde.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm lint && pnpm test` verde (test suite existente segue passando; este plano não adiciona teste unitário — a verificação de runtime é manual/smoke por exigir sessão Clerk real vs anônima, ver 03-VALIDATION.md §Manual-Only).
- Nenhum arquivo deste plano colide com 03-02 (preview-site/i18n) nem 03-03 (schema/outreach) — sem overlap de wave.
</verification>

<success_criteria>
- recordOpen ignora a abertura do próprio dono (identity.subject === preview.orgId) e conta a de qualquer caller sem identidade.
- PreviewTracker e a rota pública permanecem sem mudança.
- typecheck/lint verdes.
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-tracking-composer-e-localiza-o/03-01-SUMMARY.md`.
</output>
