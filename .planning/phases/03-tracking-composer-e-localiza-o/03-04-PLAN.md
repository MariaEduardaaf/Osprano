---
phase: 03-tracking-composer-e-localiza-o
plan: 04
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - src/components/outreach-composer.tsx
  - src/components/crm/lead-detail.tsx
  - src/app/(app)/outreach/page.tsx
autonomous: true
requirements: [TRCK-02, OUTR-01]
nyquist_compliant: true

must_haves:
  truths:
    - "O composer persiste subject/body via updateDraft ANTES de send, de markSent e ao copiar — o que se vê é o que sai"
    - "Ao reabrir a aba Abordagem com um rascunho salvo, o composer pré-preenche subject/body sem gastar chamada de IA"
    - "A hidratação do composer não sobrescreve o que o usuário está digitando (guard de useRef, hidrata uma vez)"
    - "O usuário marca 'respondeu' na outbox (itens sent/opened) e no lead-detail; e registra opt-out no lead-detail com confirmação"
  artifacts:
    - path: "src/components/outreach-composer.tsx"
      provides: "persistência via updateDraft + pré-preenchimento de getForLead com guard de hidratação"
      contains: "updateDraft"
    - path: "src/components/crm/lead-detail.tsx"
      provides: "botões Respondeu (markReplied) e Pediu opt-out (suppress + confirm) na aba Abordagem"
      contains: "markReplied"
    - path: "src/app/(app)/outreach/page.tsx"
      provides: "ação de linha 'Respondeu' para itens sent/opened"
      contains: "markReplied"
  key_links:
    - from: "src/components/outreach-composer.tsx"
      to: "api.outreach.updateDraft"
      via: "await updateDraft antes de send/markSent; fire-and-forget no copiar"
      pattern: "updateDraft\\(\\{ leadId, subject, body \\}\\)"
    - from: "src/components/outreach-composer.tsx"
      to: "api.outreach.getForLead"
      via: "useQuery + useRef hydration guard para pré-preencher uma vez"
      pattern: "hydrated"
    - from: "src/app/(app)/outreach/page.tsx"
      to: "api.outreach.markReplied"
      via: "botão de linha para itens sent/opened"
      pattern: "markReplied"
---

<objective>
Ligar a UI de TRCK-02 e OUTR-01 às mutations criadas no plano 03-03 (wave 1). Três costuras: (1) o `OutreachComposer` passa a chamar `updateDraft` antes de `send`/`markSent` e ao copiar (o que se vê é o que sai) e a pré-preencher a partir de `getForLead` quando já há rascunho salvo — com guard de `useRef` para não sobrescrever a digitação em curso (pitfall de hidratação reativa achado na pesquisa); (2) a aba Abordagem do `lead-detail` ganha os botões "Respondeu" (markReplied) e "Pediu opt-out" (suppress da Fase 2 + `window.confirm`), fechando o item deferido da Fase 2; (3) a outbox ganha a ação de linha "Respondeu" para itens sent/opened. Wave 2 porque depende das mutations de 03-03.

Purpose: O funil respondeu funciona fora do demo, o opt-out manual existe, e nenhuma edição do composer se perde.
Output: composer persistente + pré-preenchido; botões respondeu/opt-out no lead-detail; ação respondeu na outbox.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tracking-composer-e-localiza-o/03-CONTEXT.md
@.planning/phases/03-tracking-composer-e-localiza-o/03-RESEARCH.md
@.planning/phases/03-tracking-composer-e-localiza-o/03-03-PLAN.md

<interfaces>
<!-- Mutations disponíveis (criadas em 03-03) e queries existentes. Use direto. -->

Do plano 03-03 (já no codegen quando este plano roda):
- `api.outreach.markReplied({ leadId })` → grava status replied + repliedAt + evento reply; lança pt-BR se não há row.
- `api.outreach.updateDraft({ leadId, subject, body })` → patch de subject/body; lança "Nenhum rascunho encontrado..." se não há row; NÃO mexe no rodapé.

Já existentes:
- `api.outreach.getForLead({ leadId })` (convex/outreach.ts:11-22) → retorna o `Doc<"outreach">` completo (subject/body/status/...) ou null.
- `api.outreach.suppress({ leadId })` (convex/outreach.ts:192-207, Fase 2, ainda SEM UI) → registra supressão manual; lança "Lead sem email para suprimir." se não há email.
- `api.outreach.markSent`, `api.outreach.send`, `api.outreach.draft` — inalterados.

Estado atual do composer (`src/components/outreach-composer.tsx`): state `subject/body/open/busy/msg`; helper `run(kind, fn)`; botão "Escrever com IA" chama `draft` → seta subject/body → setOpen(true); botões Copiar/Marcar enviado/Enviar via Resend. Já importa `useAction, useMutation` de convex/react e `useState` de react.

Estado atual da aba Abordagem (`src/components/crm/lead-detail.tsx:270-285`):
```tsx
function ApproachTab({ lead }: { lead: Doc<"leads"> }) {
  return (
    <div className="space-y-4">
      {!lead.emailable && ( /* aviso de não-abordável */ )}
      <OutreachComposer leadId={lead._id} hasEmail={!!lead.email} />
    </div>
  );
}
```
lead-detail já importa `useMutation` (linha 4), `useState` (linha 3), `api` (linha 5), e ícones de react-icons/md (linhas 7-16).

Estado atual da outbox (`src/app/(app)/outreach/page.tsx`): importa só `useQuery` (linha 4); cada item tem `it.leadId` (tipado `Id<"leads">`), `it.status`, `it.previewToken`; último `<td>` (linhas 177-190) é a coluna "Prévia". `MdOutlineReply` JÁ está importado (usado no map STATUS).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composer persiste (updateDraft) + pré-preenche (getForLead) com guard de hidratação (OUTR-01)</name>
  <files>src/components/outreach-composer.tsx</files>
  <read_first>
    - src/components/outreach-composer.tsx (o componente inteiro — state, run(), os 3 botões copy/mark/send)
    - src/components/preview-tracker.tsx (o guard `fired` de useRef já usado no repo para efeito one-time — mesmo padrão da hidratação)
    - .planning/phases/03-tracking-composer-e-localiza-o/03-RESEARCH.md (§Pattern 4 e §Pitfall 2 — useQuery reativo sobrescreve digitação sem guard de useRef)
    - .planning/phases/03-tracking-composer-e-localiza-o/03-CONTEXT.md (§OUTR-01)
  </read_first>
  <action>
    Editar `src/components/outreach-composer.tsx`:

    1. Ajustar imports:
    - de `react`: `import { useEffect, useRef, useState } from "react";`
    - de `convex/react`: `import { useAction, useMutation, useQuery } from "convex/react";`

    2. Adicionar a mutation e o pré-preenchimento (depois das declarações de draft/send/markSent):
    ```ts
    const updateDraft = useMutation(api.outreach.updateDraft);
    const existing = useQuery(api.outreach.getForLead, { leadId });
    const hydrated = useRef(false);

    useEffect(() => {
      if (hydrated.current || existing === undefined) return; // ainda carregando
      hydrated.current = true;
      if (existing && (existing.subject || existing.body)) {
        setSubject(existing.subject ?? "");
        setBody(existing.body ?? "");
        setOpen(true); // pré-preenchido, sem gastar IA
      }
    }, [existing]);
    ```
    O botão "Escrever com IA" continua setando state direto do retorno da action (sobrescrita explícita do usuário — NÃO passa pelo guard).

    3. Persistir antes de cada saída:
    - Botão "Marcar enviado" (run "mark"):
    ```ts
    await updateDraft({ leadId, subject, body });
    await markSent({ leadId });
    setMsg("Marcado como enviado.");
    ```
    - Botão "Enviar via Resend" (run "send"):
    ```ts
    await updateDraft({ leadId, subject, body });
    await send({ leadId });
    setMsg("Enviado via Resend.");
    ```
    - Botão "Copiar" (run "copy") — persiste fire-and-forget, sem bloquear a cópia:
    ```ts
    await navigator.clipboard.writeText(`${subject}\n\n${body}`);
    void updateDraft({ leadId, subject, body }).catch(() => {});
    setMsg("Copiado — envie do seu email.");
    ```
    (O row sempre existe quando o form está aberto: ou o "Escrever com IA" já criou via upsertDraft, ou a hidratação só abre quando existe row com conteúdo — então updateDraft nunca cai no erro de row inexistente por caminho normal.)
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "useQuery(api.outreach.getForLead" src/components/outreach-composer.tsx`
    - `grep -q "hydrated = useRef(false)" src/components/outreach-composer.tsx`
    - `grep -c "updateDraft({ leadId, subject, body })" src/components/outreach-composer.tsx` retorna >= 3 (send, mark, copy)
    - Em "send" e "mark", `updateDraft` aparece ANTES da chamada de `send({ leadId })`/`markSent({ leadId })` (conferir no diff a ordem)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>O composer pré-preenche de getForLead uma única vez (guard useRef), sem gastar IA, e persiste subject/body via updateDraft antes de send, de markSent e ao copiar; typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 2: Botões Respondeu + Pediu opt-out na aba Abordagem (TRCK-02)</name>
  <files>src/components/crm/lead-detail.tsx</files>
  <read_first>
    - src/components/crm/lead-detail.tsx (ApproachTab linhas 270-285; o padrão de import de ícones no topo; useMutation/useState já importados)
    - src/components/whatsapp-followup.tsx (padrão run/busy/msg de botões — replicar o mesmo estilo)
    - .planning/phases/03-tracking-composer-e-localiza-o/03-CONTEXT.md (§TRCK-02 — botões junto do composer; confirm simples para opt-out)
  </read_first>
  <action>
    Editar `src/components/crm/lead-detail.tsx`:

    1. Adicionar ícones ao import de react-icons/md (linhas 7-16): `MdOutlineReply` e `MdBlock`.

    2. Reescrever `ApproachTab` para incluir os dois botões abaixo do composer:
    ```tsx
    function ApproachTab({ lead }: { lead: Doc<"leads"> }) {
      const markReplied = useMutation(api.outreach.markReplied);
      const suppress = useMutation(api.outreach.suppress);
      const [busy, setBusy] = useState<string | null>(null);
      const [msg, setMsg] = useState<string | null>(null);

      async function run(kind: string, fn: () => Promise<void>) {
        setBusy(kind);
        setMsg(null);
        try {
          await fn();
        } catch (e) {
          setMsg(e instanceof Error ? e.message : "Falha");
        } finally {
          setBusy(null);
        }
      }

      return (
        <div className="space-y-4">
          {!lead.emailable && (
            /* MANTER exatamente o aviso existente de "não é abordável por email frio" */
          )}
          <OutreachComposer leadId={lead._id} hasEmail={!!lead.email} />

          <div className="flex flex-wrap items-center gap-2 border-t border-border pt-3">
            <button
              onClick={() =>
                run("replied", async () => {
                  await markReplied({ leadId: lead._id });
                  setMsg("Marcado como respondeu.");
                })
              }
              disabled={busy === "replied"}
              className="inline-flex items-center gap-1.5 rounded-md border border-border px-3 py-1.5 text-xs font-semibold hover:bg-surface-2 disabled:opacity-50"
            >
              <MdOutlineReply size={14} />
              Respondeu
            </button>
            {lead.email && (
              <button
                onClick={() =>
                  run("optout", async () => {
                    if (!window.confirm("Registrar opt-out deste contato? Ele deixará de receber emails desta org.")) return;
                    await suppress({ leadId: lead._id });
                    setMsg("Opt-out registrado.");
                  })
                }
                disabled={busy === "optout"}
                className="inline-flex items-center gap-1.5 rounded-md border border-border px-3 py-1.5 text-xs font-semibold text-muted hover:bg-surface-2 disabled:opacity-50"
              >
                <MdBlock size={14} />
                Pediu opt-out
              </button>
            )}
          </div>
          {msg && <p className="text-xs text-muted">{msg}</p>}
        </div>
      );
    }
    ```
    Preservar o bloco de aviso `!lead.emailable` exatamente como está hoje. NÃO mudar o estágio do lead ao marcar respondeu (só status do outreach).
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "api.outreach.markReplied" src/components/crm/lead-detail.tsx`
    - `grep -q "api.outreach.suppress" src/components/crm/lead-detail.tsx`
    - `grep -q "window.confirm" src/components/crm/lead-detail.tsx` (confirmação antes do opt-out)
    - `grep -q "Respondeu" src/components/crm/lead-detail.tsx && grep -q "Pediu opt-out" src/components/crm/lead-detail.tsx`
    - `grep -q "MdOutlineReply\|MdBlock" src/components/crm/lead-detail.tsx`
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>A aba Abordagem mostra "Respondeu" (chama markReplied) e "Pediu opt-out" (window.confirm + suppress, só quando lead.email existe) abaixo do composer, com estado busy/msg; o aviso de não-abordável é preservado; typecheck verde.</done>
</task>

<task type="auto">
  <name>Task 3: Ação de linha "Respondeu" na outbox para itens sent/opened (TRCK-02)</name>
  <files>src/app/(app)/outreach/page.tsx</files>
  <read_first>
    - src/app/(app)/outreach/page.tsx (o map de linhas da tabela; o último `<td>` "Prévia" linhas 177-190; imports no topo)
    - .planning/phases/03-tracking-composer-e-localiza-o/03-CONTEXT.md (§TRCK-02 — ação na linha para itens sent/opened)
  </read_first>
  <action>
    Editar `src/app/(app)/outreach/page.tsx`:

    1. Trocar o import de convex/react para incluir a mutation: `import { useQuery, useMutation } from "convex/react";`

    2. Dentro de `OutreachPage`, adicionar a mutation e o estado de busy por linha:
    ```ts
    const markReplied = useMutation(api.outreach.markReplied);
    const [busyLead, setBusyLead] = useState<string | null>(null);
    ```

    3. No último `<td>` de cada linha (o da "Prévia", linhas 177-190), envolver o conteúdo num flex e adicionar o botão de resposta antes do link de prévia, visível só para status sent/opened:
    ```tsx
    <td className="px-4 py-3 text-right">
      <div className="inline-flex items-center gap-3">
        {(it.status === "sent" || it.status === "opened") && (
          <button
            onClick={async () => {
              setBusyLead(it.leadId);
              try {
                await markReplied({ leadId: it.leadId });
              } finally {
                setBusyLead(null);
              }
            }}
            disabled={busyLead === it.leadId}
            className="inline-flex items-center gap-1 text-[11px] font-semibold text-hot hover:underline disabled:opacity-50"
          >
            <MdOutlineReply size={12} /> Respondeu
          </button>
        )}
        {it.previewToken ? (
          /* MANTER o link "Abrir" existente exatamente como está */
        ) : (
          <span className="text-faint">—</span>
        )}
      </div>
    </td>
    ```
    `it.leadId` já é tipado como `Id<"leads">` pelo retorno da query — sem cast. Preservar o link "Abrir" existente dentro do novo wrapper.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "useMutation" src/app/(app)/outreach/page.tsx`
    - `grep -q "api.outreach.markReplied" src/app/(app)/outreach/page.tsx`
    - `grep -q 'it.status === "sent" || it.status === "opened"' src/app/(app)/outreach/page.tsx` (ação só para esses status)
    - `grep -q "busyLead" src/app/(app)/outreach/page.tsx`
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>Cada linha de item sent/opened mostra um botão "Respondeu" que chama markReplied com estado busy por linha; o link de prévia é preservado; typecheck verde.</done>
</task>

</tasks>

<verification>
- `npx convex codegen && pnpm typecheck && pnpm lint && pnpm test` verde (as mudanças são UI; sem novo teste unit — verificação de runtime é manual/smoke via `npx convex dev` + UI, ver 03-VALIDATION.md §Manual-Only: editar corpo → enviar → conferir persistido; marcar respondeu → outbox mostra replied com timestamp real).
- Depende de 03-03 (markReplied/updateDraft no codegen). Único plano da wave 2 — sem overlap.
</verification>

<success_criteria>
- Composer persiste antes de qualquer saída e pré-preenche sem gastar IA, sem clobbering.
- Botões Respondeu/opt-out no lead-detail e ação Respondeu na outbox, ligados às mutations reais.
- codegen/typecheck/lint/test verdes.
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-tracking-composer-e-localiza-o/03-04-SUMMARY.md`.
</output>
