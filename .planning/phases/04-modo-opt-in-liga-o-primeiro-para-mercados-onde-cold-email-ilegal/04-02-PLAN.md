---
phase: 04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - convex/places.ts
  - convex/foursquare.ts
autonomous: true
requirements: [OPTIN-01]

must_haves:
  truths:
    - "Buscar leads na Espanha (ou IT/PT/DE/DK/CH) passa pelo gate de descoberta sem erro de 'fora do escopo'"
    - "Buscar um mercado nem-launch-nem-opt-in (ex.: FR) ainda é bloqueado com mensagem clara"
    - "Todo lead inserido de mercado opt-in continua nascendo emailable=false (via isEmailable inalterado no insertDiscovered)"
  artifacts:
    - path: "convex/places.ts"
      provides: "gate de busca via isSearchableMarket"
      contains: "isSearchableMarket"
    - path: "convex/foursquare.ts"
      provides: "gate de busca via isSearchableMarket"
      contains: "isSearchableMarket"
  key_links:
    - from: "convex/places.ts (action search)"
      to: "isSearchableMarket (domain.ts)"
      via: "primeira checagem após requireOrgId"
      pattern: "isSearchableMarket\\(country\\)"
    - from: "convex/foursquare.ts (action search)"
      to: "isSearchableMarket (domain.ts)"
      via: "primeira checagem após requireOrgId"
      pattern: "isSearchableMarket\\(country\\)"
---

<objective>
Abrir a descoberta (Google Places + Foursquare) para os mercados opt-in, trocando o gate de `isLaunchMarket` por `isSearchableMarket` nas duas actions de busca.

Purpose: OPTIN-01 exige que "mercado pesquisável" ≠ "mercado emailável". A emailabilidade continua governada pelo isEmailable (inalterado); só a descoberta se amplia.
Output: `convex/places.ts` e `convex/foursquare.ts` gateando por isSearchableMarket, com mensagem de erro atualizada.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-RESEARCH.md

<interfaces>
<!-- Fornecido pela Wave 1 (04-01) em convex/lib/domain.ts: -->
```typescript
export function isSearchableMarket(countryCode: string): boolean; // launch + opt-in
export const MARKETS: Record<string, Market>;
```
<!-- Estado atual do gate (IDÊNTICO nas duas actions, places.ts:37-40 e foursquare.ts:36-39): -->
```typescript
import { isLaunchMarket, MARKETS, clampDiscoveryCount } from "./lib/domain";
if (!isLaunchMarket(country)) {
  const name = MARKETS[country]?.name ?? country;
  throw new Error(`${name} está fora do escopo compliant (cold email só em mercados opt-out).`);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 04-02-T1: Trocar gate de descoberta para isSearchableMarket (places + foursquare)</name>
  <files>convex/places.ts, convex/foursquare.ts</files>
  <read_first>
    - convex/places.ts (arquivo inteiro — import linha 5, gate linha 37-40; NÃO tocar no clampDiscoveryCount/reserve/refund, no regionCode nem no languageCode)
    - convex/foursquare.ts (arquivo inteiro — import linha 5, gate linha 36-39)
    - .planning/phases/04-.../04-RESEARCH.md §Pattern 1 (as duas trocas exatas) e §Pitfall 5 (regionCode/languageCode não mudam — ES/IT/PT/DE/DK/CH usam código ISO como GB já usa)
  </read_first>
  <action>
    Em AMBOS convex/places.ts e convex/foursquare.ts (mudança idêntica):
    1. No import de "./lib/domain", trocar `isLaunchMarket` por `isSearchableMarket` (manter `MARKETS, clampDiscoveryCount`):
       `import { isSearchableMarket, MARKETS, clampDiscoveryCount } from "./lib/domain";`
    2. No gate (primeira checagem após `const country = args.countryCode.toUpperCase();`), trocar a função e a mensagem pt-BR:
       ```typescript
       if (!isSearchableMarket(country)) {
         const name = MARKETS[country]?.name ?? country;
         throw new Error(`${name} ainda não está disponível para busca.`);
       }
       ```
    3. NÃO alterar mais nada: `regionCode: country` e `languageCode: "en"` (places.ts) e `near: \`${city}, ${country}\`` (foursquare.ts) já funcionam para mercados EU não-anglófonos (precedente NL/SE/NO). O clamp de quota, reserve/refund e o loop de paginação ficam intactos.
    Observação de escopo: NÃO tocar em src/app/page.tsx (footer de marketing) nem em src/components/crm/create-lead-modal.tsx — usam o array LAUNCH_MARKETS para display/dropdown, fora do escopo desta fase (Pitfall 7).
  </action>
  <verify>
    <automated>grep -c "isLaunchMarket" convex/places.ts convex/foursquare.ts; grep -n "isSearchableMarket(country)" convex/places.ts convex/foursquare.ts; grep -n "ainda não está disponível para busca" convex/places.ts convex/foursquare.ts; npx convex codegen 2>&1 | tail -2 && pnpm typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isLaunchMarket" convex/places.ts` retorna 0 (nenhuma referência restante)
    - `grep -c "isLaunchMarket" convex/foursquare.ts` retorna 0
    - `grep "isSearchableMarket(country)" convex/places.ts` casa; idem convex/foursquare.ts
    - `grep "ainda não está disponível para busca" convex/places.ts` casa; idem foursquare.ts
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>Ambas as actions de busca gateiam por isSearchableMarket com mensagem pt-BR nova; typecheck verde; nenhuma referência a isLaunchMarket sobra em places.ts/foursquare.ts.</done>
</task>

</tasks>

<verification>
- grep negativo: `isLaunchMarket` não aparece em convex/places.ts nem convex/foursquare.ts.
- `pnpm typecheck` verde.
- Manual (verify-work, exige GOOGLE_PLACES_API_KEY): buscar "restaurant · Madrid" em dev → leads inseridos com emailable=false.
</verification>

<success_criteria>
- Descoberta funciona para os 6 mercados opt-in; mercados fora das duas listas seguem bloqueados; emailabilidade dos leads inalterada (governada pelo isEmailable no insertDiscovered).
</success_criteria>

<output>
After completion, create `.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-02-SUMMARY.md`
</output>
