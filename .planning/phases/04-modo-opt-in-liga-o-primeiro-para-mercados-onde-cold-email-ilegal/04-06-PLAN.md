---
phase: 04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal
plan: 06
type: execute
wave: 4
depends_on: ["04-01", "04-05"]
files_modified:
  - src/app/(app)/leads/page.tsx
  - src/components/crm/lead-detail.tsx
autonomous: true
requirements: [OPTIN-02, OPTIN-06]

must_haves:
  truths:
    - "A página de Leads tem as abas 'Email primeiro' (default) e 'Ligação primeiro'"
    - "Na aba 'Email primeiro', o select de países mostra os mercados launch e a lista filtra leads opt-out; na 'Ligação primeiro', o select mostra os mercados opt-in e a lista filtra leads opt-in, renderizando o card com variant='call'"
    - "A aba 'Ligação primeiro' mostra um banner discreto de validação jurídica pendente (OPTIN-06)"
    - "No detalhe do CRM, o aviso de compliance acima do composer some quando o lead tem consentimento registrado (usa canContactByEmail)"
    - "O card da aba 'Ligação primeiro' recebe o slot `action` (GeneratePreviewButton) igual ao da aba de email; a visibilidade do fluxo de email é decidida pelo gating INTERNO do card (04-05), nunca omitindo o slot no pai"
  artifacts:
    - path: "src/app/(app)/leads/page.tsx"
      provides: "tabs Email/Ligação, select por regime, filtro client-side por OPT_IN_MARKETS, variant='call' no card, banner de validação jurídica, empty state do fluxo"
      contains: "Ligação primeiro"
    - path: "src/components/crm/lead-detail.tsx"
      provides: "banner do ApproachTab via canContactByEmail (destrava visualmente após consentimento)"
      contains: "canContactByEmail"
  key_links:
    - from: "src/app/(app)/leads/page.tsx (aba Ligação)"
      to: "LeadCard variant='call'"
      via: "render condicional por aba + filtro OPT_IN_MARKETS.includes(countryCode)"
      pattern: "variant=\"call\"|OPT_IN_MARKETS"
    - from: "src/components/crm/lead-detail.tsx (ApproachTab)"
      to: "canContactByEmail(lead)"
      via: "condição do banner de aviso"
      pattern: "canContactByEmail\\(lead\\)"
---

<objective>
Fechar a UI do modo opt-in: as abas "Email primeiro"/"Ligação primeiro" na página de Leads (com select por regime, filtro por mercado, variante de card e banner de validação jurídica) e a consistência do banner de compliance no detalhe do CRM.

Purpose: OPTIN-02 (abas + card call por regime, sem cold email na aba de ligação) e OPTIN-06 (aviso "validação jurídica pendente" na aba opt-in). Ajuste incorporado: o banner do ApproachTab que lê !lead.emailable passa a usar canContactByEmail, senão mostra "fora do escopo" enquanto o composer já funciona (Pitfall 4).
Output: `src/app/(app)/leads/page.tsx` com tabs/filtro/banner e `src/components/crm/lead-detail.tsx` com o banner corrigido.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-CONTEXT.md
@.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-RESEARCH.md

<interfaces>
<!-- Fornecido pela Wave 1 (04-01), importável no client via @convex/lib/domain: -->
```typescript
export const LAUNCH_MARKETS: string[];   // ["GB","NL","IE","SE","NO"]
export const OPT_IN_MARKETS: string[];    // ["ES","IT","PT","DE","DK","CH"]
export const MARKETS: Record<string, Market>; // Market.legalReview?: "pending" | "validated"
export function canContactByEmail(lead): boolean;
```
<!-- LeadCard (04-05) agora aceita `variant?: "email" | "call"`; no modo call renderiza Ligar/Script/Consentimento e faz o gating INTERNO do slot action (esconde até haver consentimento). Contrato: o pai SEMPRE passa `action`, nas duas abas — quem decide a visibilidade é o card, não o pai. -->
<!-- Estado atual — src/app/(app)/leads/page.tsx: select usa LAUNCH_MARKETS (linha 87), CITIES_BY_COUNTRY[country] (linha 111), lista renderiza <LeadCard action={<GeneratePreviewButton .../>} /> (linha 190-199); useQuery(api.leads.list, {}) traz TODOS os leads da org. -->
<!-- Estado atual — src/components/crm/lead-detail.tsx: ApproachTab banner linha 292 `{!lead.emailable && (...)}`; há OUTRO uso em linha 194 (painel de status legal) que NÃO deve mudar. -->
<!-- Padrão de pílula/tab a reusar — src/app/(app)/outreach/page.tsx FILTERS (state-driven, className condicional). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 04-06-T1: Abas Email/Ligação, filtro por regime, variante e banner na página de Leads</name>
  <files>src/app/(app)/leads/page.tsx</files>
  <read_first>
    - src/app/(app)/leads/page.tsx (arquivo inteiro — imports linha 1-11, estados linha 13-20, select de país linha 79-92, contadores linha 151-157, grid de cards linha 189-201, empty state linha 183-187)
    - src/app/(app)/outreach/page.tsx (padrão de pílula state-driven FILTERS — copiar o shape visual das abas)
    - .planning/phases/04-.../04-CONTEXT.md §OPTIN-02 (o que a aba controla: select, filtro, variante) e §OPTIN-06 (banner) e §Claude's Discretion (copy do banner/empty state)
    - .planning/phases/04-.../04-RESEARCH.md §Pattern 4 (filtro client-side, sem query nova)
  </read_first>
  <action>
    Em src/app/(app)/leads/page.tsx:
    1. Import: adicionar `OPT_IN_MARKETS` (e `canContactByEmail` NÃO é necessário aqui) ao import de "@convex/lib/domain".
    2. Estado da aba: `const [tab, setTab] = useState<"email" | "call">("email");`. Derivar `const markets = tab === "call" ? OPT_IN_MARKETS : LAUNCH_MARKETS;`.
    3. Tabs (pílulas) no topo, acima do form de busca — dois botões "Email primeiro" e "Ligação primeiro" no estilo state-driven do outreach/page.tsx (className condicional: ativo = `bg-brand text-brand-fg`, inativo = `border border-border text-muted`). Ao trocar de aba: `setTab(...)`, e resetar `country` para `markets[0]` da aba nova e `setCity("")` (evita país da outra aba preso no select).
    4. Select de país: trocar `LAUNCH_MARKETS.map(...)` (linha 87) por `markets.map(...)` para refletir a aba atual.
    5. Filtro da lista por regime (client-side, sem query nova): derivar
       `const shownLeads = (leads ?? []).filter((l) => tab === "call" ? OPT_IN_MARKETS.includes(l.countryCode) : !OPT_IN_MARKETS.includes(l.countryCode));`
       e usar `shownLeads` na renderização dos cards, nos contadores e na lógica de seleção (substituindo `leads`/`shown` onde for a lista visível — manter `saveMany`/seleção funcionando com os ids visíveis).
    6. Render dos cards: SEMPRE passar o slot `action={<GeneratePreviewButton leadId={lead._id} .../>}` ao `<LeadCard />` nas DUAS abas (contrato com o 04-05 — quem decide a visibilidade é o gating interno do card, não o pai). Na aba de ligação, adicionar `variant="call"` MANTENDO o `action`: `<LeadCard ... variant="call" action={<GeneratePreviewButton .../>} />`. NÃO omitir `action` no modo call — sem consentimento o card já esconde o slot internamente (04-05-T3); após o consentimento ele o expõe (é assim que "o email destrava"). Na aba de email, `<LeadCard ... action={<GeneratePreviewButton .../>} />` (inalterado, sem variant).
    7. Banner de validação jurídica (OPTIN-06) — visível SÓ na aba call, discreto, borda warm:
       texto sugerido: "Mercados em validação jurídica — ligação B2B permitida; email/WhatsApp só após consentimento registrado." (classe estilo `border border-warm/30 bg-warm/10 text-ink-soft`, ícone MdOutlineGavel/MdOutlineWarningAmber). Renderizar acima da lista quando `tab === "call"`.
    8. Empty state por aba: na aba call, quando não há leads opt-in, um EmptyState explicando o fluxo: "Busque um mercado opt-in ({OPT_IN_MARKETS.join(' · ')}). O fluxo é: ligar → registrar consentimento → email destrava."
    9. NÃO alterar a action de busca (`api.places.search`) — o gate já foi ampliado no 04-02; a mesma busca funciona para os países opt-in do select.
  </action>
  <verify>
    <automated>grep -n "OPT_IN_MARKETS\|Ligação primeiro\|Email primeiro\|variant=\"call\"\|validação jurídica\|GeneratePreviewButton" 'src/app/(app)/leads/page.tsx' && pnpm typecheck 2>&1 | tail -3 && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Email primeiro" 'src/app/(app)/leads/page.tsx'` e `grep "Ligação primeiro" ...` casam (as duas abas)
    - `grep 'variant="call"' 'src/app/(app)/leads/page.tsx'` casa (card no modo call na aba de ligação)
    - `grep "OPT_IN_MARKETS.includes" 'src/app/(app)/leads/page.tsx'` casa (filtro por regime)
    - `grep "validação jurídica" 'src/app/(app)/leads/page.tsx'` casa (banner OPTIN-06)
    - `grep "GeneratePreviewButton" 'src/app/(app)/leads/page.tsx'` casa E o slot `action={<GeneratePreviewButton .../>}` é passado ao `<LeadCard />` também no ramo `variant="call"` (contrato com 04-05) — inspeção: o `action` NÃO está condicionado a `tab === "email"` nem omitido no card da aba de ligação
    - `pnpm typecheck` exit 0; `pnpm lint` sem erros novos
  </acceptance_criteria>
  <done>Página de Leads com abas Email/Ligação, select+filtro por regime, card variant="call" na aba de ligação (com o slot action passado — gating de visibilidade é interno ao card), banner de validação jurídica e empty state do fluxo; typecheck/lint verdes.</done>
</task>

<task type="auto">
  <name>Task 04-06-T2: Banner do ApproachTab via canContactByEmail (consistência CRM)</name>
  <files>src/components/crm/lead-detail.tsx</files>
  <read_first>
    - src/components/crm/lead-detail.tsx (imports linha 7-19; ApproachTab linha 272+; banner de aviso linha 292 `{!lead.emailable && (...)}`; painel de status legal linha 194 — NÃO mexer)
    - .planning/phases/04-.../04-RESEARCH.md §Pitfall 4 (o banner do ApproachTab fica obsoleto para lead consentido; trocar por canContactByEmail é a correção de 1 condição)
    - .planning/phases/04-.../04-CONTEXT.md §Ajustes incorporados (lead-detail usa canContactByEmail)
  </read_first>
  <action>
    Em src/components/crm/lead-detail.tsx:
    1. No import de "@convex/lib/domain" (hoje `import { PIPELINE_STAGES, MARKETS, type Stage } from "@convex/lib/domain";`), adicionar `canContactByEmail`.
    2. No ApproachTab, trocar a condição do banner de aviso (linha ~292) de `{!lead.emailable && (` para `{!canContactByEmail(lead) && (` — assim, quando o lead tiver contactOptInAt (consentimento), o aviso "não é abordável por email frio... Prefira uma ligação" some, coerente com o composer que já funciona.
    3. NÃO alterar o outro uso `lead.emailable` no painel de status legal (linha ~194) — aquele painel reporta a defensibilidade legal crua (mercado/forma jurídica/tipo de contato) e deve continuar mostrando o status de emailabilidade de origem. Apenas o banner de AÇÃO acima do composer muda.
  </action>
  <verify>
    <automated>grep -n "canContactByEmail" src/components/crm/lead-detail.tsx && grep -c "!lead.emailable" src/components/crm/lead-detail.tsx && pnpm typecheck 2>&1 | tail -3 && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "canContactByEmail" src/components/crm/lead-detail.tsx` casa no import e na condição `{!canContactByEmail(lead) && (`
    - `grep -c "!lead.emailable" src/components/crm/lead-detail.tsx` retorna 0 (o único `!lead.emailable`, no banner do ApproachTab, foi migrado; o uso do painel legal na linha ~194 é `lead.emailable ?`, não `!lead.emailable`, então permanece)
    - `pnpm typecheck` exit 0; `pnpm lint` sem erros novos
  </acceptance_criteria>
  <done>Banner de aviso do ApproachTab usa canContactByEmail (some após consentimento); painel de status legal intacto; typecheck/lint verdes.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck && pnpm lint` verdes.
- grep: "Email primeiro"/"Ligação primeiro"/variant="call"/OPT_IN_MARKETS.includes/"validação jurídica" em leads/page.tsx; `canContactByEmail` em lead-detail.tsx; `!lead.emailable` = 0 em lead-detail.tsx.
- Manual (verify-work, visual): aba "Ligação primeiro" mostra países opt-in no select, cards sem ação de email, banner de validação jurídica; registrar consentimento num lead ES no CRM faz o aviso do ApproachTab sumir.
</verification>

<success_criteria>
- Abas Email/Ligação com select+filtro+variante por regime, banner de validação jurídica na aba opt-in, e o aviso de compliance do CRM coerente com o consentimento.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-06-SUMMARY.md`
</output>
