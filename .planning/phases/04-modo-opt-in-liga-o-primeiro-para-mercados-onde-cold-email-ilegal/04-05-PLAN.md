---
phase: 04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-03"]
files_modified:
  - src/components/contact-opt-in-button.tsx
  - src/components/call-script-panel.tsx
  - src/components/lead-card.tsx
autonomous: true
requirements: [OPTIN-02, OPTIN-03, OPTIN-04]

must_haves:
  truths:
    - "No card variante 'call', a ação primária é Ligar (tel:) e há um botão de Script de ligação; NÃO há ação de cold email enquanto não houver consentimento — o slot action do pai só é renderizado após contactOptInAt"
    - "O usuário registra consentimento pelo card (select de origem + confirmar) e o card passa a mostrar '✓ Consentimento registrado' e a expor o fluxo normal de email (slot action do pai)"
    - "O painel de script mostra o script no idioma do mercado e a tradução pt-BR lado a lado, cada um com botão de copiar"
  artifacts:
    - path: "src/components/contact-opt-in-button.tsx"
      provides: "UI de registro de consentimento (origem phone_call/in_person/reply/other + confirmar → api.leads.recordContactOptIn)"
      contains: "recordContactOptIn"
    - path: "src/components/call-script-panel.tsx"
      provides: "gerar (api.outreach.callScript) + exibir script/tradução lado a lado + copiar"
      contains: "callScript"
    - path: "src/components/lead-card.tsx"
      provides: "prop variant?: 'email' | 'call'; rodapé de compliance de 3 estados; ações do modo call; slot action gateado internamente por contactOptInAt no modo call"
      contains: "variant"
  key_links:
    - from: "src/components/contact-opt-in-button.tsx"
      to: "api.leads.recordContactOptIn"
      via: "useMutation + confirmar"
      pattern: "recordContactOptIn"
    - from: "src/components/call-script-panel.tsx"
      to: "api.outreach.callScript"
      via: "useAction"
      pattern: "api.outreach.callScript"
    - from: "src/components/lead-card.tsx (variant call)"
      to: "CallScriptPanel + ContactOptInButton"
      via: "render no bloco de ações quando variant === 'call'"
      pattern: "CallScriptPanel|ContactOptInButton"
    - from: "src/components/lead-card.tsx (variant call, consentido)"
      to: "slot action do pai (GeneratePreviewButton, passado pelo 04-06)"
      via: "render condicionado a lead.contactOptInAt — o gating é INTERNO ao card, nunca do pai"
      pattern: "contactOptInAt"
---

<objective>
UI do modo ligação-primeiro: card com variante "call" (Ligar + Script + Registrar consentimento, sem cold email), o painel de script bilíngue, e o botão de consentimento — reusando o padrão visual do WhatsAppFollowup e do GeneratePreviewButton.

Purpose: OPTIN-02 (card prioriza telefone/script, sem ação de email), OPTIN-03 (painel script + tradução pt-BR lado a lado — requisito porque a dona não fala os idiomas dos mercados), OPTIN-04 (registro de consentimento destrava o email no card).
Output: dois componentes novos (contact-opt-in-button.tsx, call-script-panel.tsx) e o lead-card.tsx com `variant` + rodapé de compliance de 3 estados.

Contrato com o 04-06: a página de Leads SEMPRE passa o slot `action` (GeneratePreviewButton) nas DUAS abas; é o gating interno do card (variant + contactOptInAt) que decide se o slot aparece. Sem consentimento no modo call, o slot NÃO é renderizado.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-CONTEXT.md
@.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-RESEARCH.md

<interfaces>
<!-- Backend disponível após 04-03 (api gerada pelo convex codegen): -->
```typescript
api.leads.recordContactOptIn({ leadId, source, note? }) => { optInAt: number }  // source: "phone_call"|"in_person"|"reply"|"other"
api.outreach.callScript({ leadId }) => { script: string; translation: string } // exige lead.phone + ANTHROPIC_API_KEY
```
<!-- Doc<"leads"> agora tem (04-01): contactOptInAt?, contactOptInSource?, contactOptInNote?, callScript?, callScriptPt?, callScriptAt? -->
<!-- domain.ts (04-01): canContactByEmail(lead) — importável no client via @convex/lib/domain -->
<!-- Molde de UI de opt-in — src/components/whatsapp-followup.tsx (select de OPT_IN_SOURCES + botão confirmar chamando useMutation). -->
<!-- Molde de action-button com estado busy/resultado — src/components/generate-preview-button.tsx. -->
<!-- LeadCard atual — src/components/lead-card.tsx: props { lead, action?, selectable, selected, onToggle }; rodapé de compliance 2-way linha 146-155 (lead.emailable ? "Abordável por email" : "Fora do escopo compliant"); bloco de ações linha 158-177 (action slot + link "Site atual"). -->
<!-- Consumidor único do LeadCard: src/app/(app)/leads/page.tsx (04-06), que passará SEMPRE action={<GeneratePreviewButton .../>} nas duas abas. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 04-05-T1: Componente ContactOptInButton (registro de consentimento)</name>
  <files>src/components/contact-opt-in-button.tsx</files>
  <read_first>
    - src/components/whatsapp-followup.tsx (arquivo inteiro — o branch !optInAt linha 44-84 é o molde exato: select de origem + botão confirmar via useMutation)
    - .planning/phases/04-.../04-CONTEXT.md §OPTIN-02 (fluxo "Registrar consentimento") e §OPTIN-04 (vocabulário de origem)
  </read_first>
  <action>
    Criar src/components/contact-opt-in-button.tsx ("use client"), espelhando o branch de opt-in do WhatsAppFollowup, mas chamando `api.leads.recordContactOptIn` e com o vocabulário de origem do OPTIN-04:
    - Props: `{ leadId: Id<"leads">; optInAt?: number }`.
    - Constante local de origens:
      ```typescript
      const CONTACT_SOURCES = [
        { value: "phone_call", label: "Falou por telefone" },
        { value: "in_person", label: "Pessoalmente" },
        { value: "reply", label: "Respondeu" },
        { value: "other", label: "Outro" },
      ] as const;
      ```
    - `const recordOptIn = useMutation(api.leads.recordContactOptIn);` + estados `open`, `source` (default CONTACT_SOURCES[0].value), `busy`, `msg`.
    - Se `optInAt` já existe: renderizar um selo estático "✓ Consentimento registrado" (texto pequeno, cor de sucesso/brand) — NÃO renderizar o formulário.
    - Se `!optInAt`: um botão colapsado "Registrar consentimento" que abre o form (select de CONTACT_SOURCES + botão "Registrar") — copiar as classes/tamanhos do whatsapp-followup para consistência. Ao confirmar: `await recordOptIn({ leadId, source })` com try/catch setando msg pt-BR em falha.
    - Acessibilidade: `aria-label="Origem do consentimento"` no select; textos pt-BR.
    - NÃO chamar nenhuma action de email aqui; o destravamento do email é responsabilidade do lead-card (T3) ao reagir a lead.contactOptInAt.
  </action>
  <verify>
    <automated>test -f src/components/contact-opt-in-button.tsx && grep -n "recordContactOptIn\|phone_call\|in_person\|Registrar consentimento" src/components/contact-opt-in-button.tsx && pnpm typecheck 2>&1 | tail -3 && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/contact-opt-in-button.tsx` OK
    - `grep "api.leads.recordContactOptIn" src/components/contact-opt-in-button.tsx` casa
    - `grep "phone_call" ... && grep "in_person" ... && grep "reply" ... && grep "other"` casam (vocabulário OPTIN-04)
    - `pnpm typecheck` exit 0; `pnpm lint` sem erros novos
  </acceptance_criteria>
  <done>ContactOptInButton renderiza o form de origem+confirmar (chamando recordContactOptIn) quando sem consentimento, e um selo quando já consentido; typecheck/lint verdes.</done>
</task>

<task type="auto">
  <name>Task 04-05-T2: Componente CallScriptPanel (gerar + script/tradução lado a lado + copiar)</name>
  <files>src/components/call-script-panel.tsx</files>
  <read_first>
    - src/components/generate-preview-button.tsx (arquivo inteiro — padrão de action-button com busy + resultado revelado)
    - .planning/phases/04-.../04-CONTEXT.md §OPTIN-03 (UI: painel/expansão com script e tradução lado a lado, botão copiar em cada um) e §specifics (a tradução pt-BR é requisito porque a dona não fala os idiomas)
    - .planning/phases/04-.../04-RESEARCH.md §Don't Hand-Roll (copiar via navigator.clipboard.writeText; sem lib)
  </read_first>
  <action>
    Criar src/components/call-script-panel.tsx ("use client"):
    - Props: `{ leadId: Id<"leads">; phone?: string; initialScript?: string; initialTranslation?: string }` (os initial* vêm de lead.callScript/callScriptPt persistidos, para não regenerar).
    - `const callScript = useAction(api.outreach.callScript);` + estados `open`, `script`/`translation` (seed com initial*), `busy`, `msg`, e um estado transiente de "copiado" por coluna.
    - Botão colapsado "Script de ligação" (ícone MdOutlineDescription/MdOutlineCall) que abre o painel.
    - Botão "Gerar script" (ou "Regenerar" se já houver script): `await callScript({ leadId })` setando script+translation; try/catch com msg pt-BR (ex.: propagar a mensagem do backend "Lead sem telefone." / "ANTHROPIC_API_KEY...").
    - Quando houver script: duas colunas lado a lado (grid md:grid-cols-2, empilha no mobile): esquerda = script (idioma do mercado, rótulo "Script"), direita = tradução (rótulo "Tradução (pt-BR)"). Cada coluna com um botão "Copiar" chamando `navigator.clipboard.writeText(...)` e mostrando "Copiado!" por ~1.5s via useState + setTimeout.
    - Estilo consistente com o design system (surface-2, border-border, textos pequenos); pt-BR em toda a copy/labels; `aria-label` nos botões de copiar.
    - Se `!phone`: desabilitar/gerar deve mostrar aviso pt-BR "Lead sem telefone." antes mesmo de chamar (ou deixar o backend recusar — preferir checagem local para UX).
  </action>
  <verify>
    <automated>test -f src/components/call-script-panel.tsx && grep -n "api.outreach.callScript\|navigator.clipboard.writeText\|Tradução\|Copiar" src/components/call-script-panel.tsx && pnpm typecheck 2>&1 | tail -3 && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/call-script-panel.tsx` OK
    - `grep "api.outreach.callScript" src/components/call-script-panel.tsx` casa
    - `grep "navigator.clipboard.writeText" src/components/call-script-panel.tsx` casa (copiar nativo, sem lib)
    - `grep "Tradução" src/components/call-script-panel.tsx` casa (coluna pt-BR presente)
    - `pnpm typecheck` exit 0; `pnpm lint` sem erros novos
  </acceptance_criteria>
  <done>CallScriptPanel gera via api.outreach.callScript, mostra script + tradução pt-BR lado a lado com copiar nativo, e aproveita initialScript/initialTranslation persistidos; typecheck/lint verdes.</done>
</task>

<task type="auto">
  <name>Task 04-05-T3: LeadCard ganha variant "call" + rodapé de compliance de 3 estados</name>
  <files>src/components/lead-card.tsx</files>
  <read_first>
    - src/components/lead-card.tsx (arquivo inteiro — props linha 36-48, rodapé compliance linha 146-155, bloco de ações linha 158-177)
    - src/components/contact-opt-in-button.tsx e src/components/call-script-panel.tsx (T1/T2 deste plano — os componentes que o card vai compor)
    - .planning/phases/04-.../04-RESEARCH.md §Pattern 5 (variant é modo de render interno, rodapé 3-way, não composição de slot) e §OPTIN-02
  </read_first>
  <action>
    Em src/components/lead-card.tsx:
    1. Adicionar `variant?: "email" | "call"` às props (default "email"). Importar `CallScriptPanel` e `ContactOptInButton`, e o ícone `MdOutlineCall` (já importado) para o botão Ligar.
    2. Rodapé de compliance — substituir o ternário 2-way (linha 146-155) por lógica de 3 estados:
       - `variant === "call" && !lead.contactOptInAt` → texto "📞 Ligação primeiro · email após consentimento" (tom neutro/muted).
       - `variant === "call" && lead.contactOptInAt` → "✓ Consentimento registrado em {data}" (formatar lead.contactOptInAt com `new Date(...).toLocaleDateString("pt-BR")`; tom brand/sucesso).
       - senão (variant "email", inalterado) → `lead.emailable ? "Abordável por email" : "Fora do escopo compliant"`.
    3. Bloco de ações — quando `variant === "call"`, o gating do slot `action` é INTERNO ao card (o pai SEMPRE passa o slot — contrato com o 04-06 — e o card decide a visibilidade):
       - **SEM consentimento (`!lead.contactOptInAt`)**: renderizar SOMENTE as ações próprias do card e **NÃO renderizar o slot `action` do pai** (nenhuma ação de cold email):
         - Botão primário **Ligar**: `<a href={`tel:${lead.phone}`}>` (só se lead.phone) com ícone MdOutlineCall, estilo de botão primário.
         - `<CallScriptPanel leadId={lead._id} phone={lead.phone} initialScript={lead.callScript} initialTranslation={lead.callScriptPt} />`.
         - `<ContactOptInButton leadId={lead._id} optInAt={lead.contactOptInAt} />`.
       - **COM consentimento (`lead.contactOptInAt` presente)**: manter Ligar + CallScriptPanel + o selo do ContactOptInButton E **TAMBÉM renderizar o slot `action` do pai** (a página de Leads passa `action={<GeneratePreviewButton .../>}` nas duas abas — 04-06). É assim que "o card passa a expor o fluxo normal de email". Se `action` não for passado (uso futuro), apenas o rodapé muda — sem crash.
       - O link "Site atual" continua com o comportamento atual em ambas as variantes (não é ação de email).
       - Envolver os cliques internos em `onClick={(e) => e.stopPropagation()}` (como o card já faz no slot action) para não disparar o toggle de seleção.
    4. Quando `variant === "email"` (default), NÃO mudar nada do comportamento atual (slot action + "Site atual").
  </action>
  <verify>
    <automated>grep -n "variant\|CallScriptPanel\|ContactOptInButton\|tel:\|Consentimento registrado\|Ligação primeiro" src/components/lead-card.tsx && grep -c "contactOptInAt" src/components/lead-card.tsx && pnpm typecheck 2>&1 | tail -3 && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'variant?: "email" | "call"' src/components/lead-card.tsx` casa (ou variant nas props)
    - `grep "CallScriptPanel" src/components/lead-card.tsx` e `grep "ContactOptInButton" src/components/lead-card.tsx` casam
    - `grep "tel:" src/components/lead-card.tsx` casa (botão Ligar)
    - `grep "Ligação primeiro" src/components/lead-card.tsx` e `grep "Consentimento registrado" src/components/lead-card.tsx` casam (rodapé 3-estados)
    - `grep -c "contactOptInAt" src/components/lead-card.tsx` retorna >= 2 (gating do rodapé E do slot action no modo call)
    - Inspeção do bloco de ações: no modo call, o render do slot `action` está condicionado a `lead.contactOptInAt` (o slot NÃO aparece sem consentimento; aparece com consentimento)
    - `pnpm typecheck` exit 0; `pnpm lint` sem erros novos
  </acceptance_criteria>
  <done>LeadCard aceita variant "call" com Ligar/Script/Consentimento; slot action do pai gateado INTERNAMENTE por contactOptInAt no modo call (escondido sem consentimento, renderizado após); rodapé de compliance de 3 estados; variante "email" inalterada; typecheck/lint verdes.</done>
</task>

</tasks>

<verification>
- `npx convex codegen && pnpm typecheck && pnpm lint` verdes (api.leads.recordContactOptIn / api.outreach.callScript existem via 04-03).
- grep: variant, CallScriptPanel, ContactOptInButton, tel:, "Consentimento registrado" presentes em lead-card.tsx; `contactOptInAt` aparece >= 2× (rodapé + gating do slot).
- Manual (verify-work, visual + exige ANTHROPIC_API_KEY p/ script): card call mostra Ligar + Script + Registrar consentimento, sem botão de email; após registrar, o GeneratePreviewButton (slot do pai) aparece no card; painel de script mostra script + tradução pt-BR.
</verification>

<success_criteria>
- Card variante call prioriza telefone e script, esconde cold email até o consentimento (gating interno do slot action), e expõe o fluxo de email após consentimento; painel bilíngue com copiar; registro de consentimento pelo card.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-05-SUMMARY.md`
</output>
