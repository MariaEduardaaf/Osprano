---
phase: 04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - convex/leads.ts
  - convex/lib/outreachAi.ts
  - convex/outreach.ts
  - tests/outreach-lang.test.ts
autonomous: true
requirements: [OPTIN-03, OPTIN-04, OPTIN-05]

must_haves:
  truths:
    - "O usuário registra consentimento de contato (origem + timestamp + nota) num lead via mutation autenticada"
    - "outreach.draft e outreach.send recusam lead de mercado opt-in SEM consentimento, com erro pt-BR — mesmo que a UI tentasse"
    - "Depois de consentimento registrado, draft/send passam a funcionar para o lead opt-in (guardrail é canContactByEmail)"
    - "O usuário gera um script de ligação por IA no idioma do mercado com tradução pt-BR, terminando com pedido de consentimento"
    - "O script é persistido no lead para não regenerar à toa; gerar script NÃO exige consentimento (a ligação é o que conquista o consentimento)"
  artifacts:
    - path: "convex/leads.ts"
      provides: "recordContactOptIn (mutation) + setCallScript (internalMutation)"
      contains: "recordContactOptIn"
    - path: "convex/lib/outreachAi.ts"
      provides: "writeCallScript(apiKey, lead) + LANG ampliado (ES/IT/PT/DE/DK/CH)"
      contains: "writeCallScript"
    - path: "convex/outreach.ts"
      provides: "gate draft/send via canContactByEmail + action callScript"
      contains: "canContactByEmail"
    - path: "tests/outreach-lang.test.ts"
      provides: "teste de paridade: LANG cobre todos os OPT_IN_MARKETS"
      contains: "LANG"
  key_links:
    - from: "convex/outreach.ts (draft/send)"
      to: "canContactByEmail (domain.ts)"
      via: "troca do check !lead.emailable"
      pattern: "canContactByEmail\\(lead\\)"
    - from: "convex/outreach.ts (action callScript)"
      to: "writeCallScript (outreachAi.ts) + internal.leads.setCallScript"
      via: "gera e persiste o script"
      pattern: "writeCallScript\\(|setCallScript"
    - from: "convex/leads.ts (recordContactOptIn)"
      to: "events (contact_opt_in)"
      via: "insere evento + patch dos campos contactOptIn* no lead"
      pattern: "contact_opt_in"
---

<objective>
Backend do modo opt-in: registrar consentimento de contato, gerar script de ligação por IA, e mover o guardrail de email de `emailable` para `canContactByEmail` — tudo server-side.

Purpose: OPTIN-03 (script de ligação), OPTIN-04 (consentimento generalizado que destrava email) e OPTIN-05 (guardrail server-side). O consentimento explícito é base legal própria; o script de ligação é o mecanismo que conquista esse consentimento.
Output: `convex/leads.ts` (recordContactOptIn + setCallScript), `convex/lib/outreachAi.ts` (writeCallScript + LANG), `convex/outreach.ts` (draft/send via canContactByEmail + callScript action), e um teste puro de paridade do LANG.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-CONTEXT.md
@.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-RESEARCH.md

<interfaces>
<!-- Fornecido pela Wave 1 (04-01): -->
```typescript
// convex/lib/domain.ts
export function canContactByEmail(lead: { emailable?: boolean | null; contactOptInAt?: number | null }): boolean;
// convex/schema.ts (leads): contactOptInAt/Source/Note?, callScript/callScriptPt/callScriptAt?; events.type inclui "contact_opt_in"
```
<!-- Molde EXISTENTE a copiar — convex/leads.ts:174-196 (recordWaOptIn): -->
```typescript
export const recordWaOptIn = mutation({
  args: { leadId: v.id("leads"), source: v.string(), note: v.optional(v.string()) },
  handler: async (ctx, { leadId, source, note }) => {
    const orgId = await requireOrgId(ctx);
    const lead = await ctx.db.get(leadId);
    if (!lead || lead.orgId !== orgId) throw new Error("Lead não encontrado");
    const now = Date.now();
    await ctx.db.patch(leadId, { waOptInAt: now, waOptInSource: source });
    await ctx.db.insert("events", { orgId, type: "wa_opt_in", leadId, at: now, meta: { source, ...(note ? { note } : {}) } });
    return { optInAt: now };
  },
});
```
<!-- Molde EXISTENTE — convex/lib/outreachAi.ts writeEmail (LANG linha 5-11, SIGNAL_TEXT módulo-privado linha 13-20, fetch/fallback linha 59-88). -->
<!-- Molde EXISTENTE — convex/outreach.ts draft (137-170, gate linha 144) e send (248-307, gate linha 254). Manter as checagens de supressão (isSuppressed) intactas. -->
<!-- getInternal já existe: internal.leads.getInternal. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 04-03-T1: Consentimento de contato + persistência de script em leads.ts</name>
  <files>convex/leads.ts</files>
  <read_first>
    - convex/leads.ts (arquivo inteiro — recordWaOptIn linha 174-196 é o molde; getInternal linha 202-205; applyScore linha 297-320; imports/mutation/internalMutation no topo)
    - .planning/phases/04-.../04-RESEARCH.md §Pattern 2 (recordContactOptIn) e §Pattern 3 (setCallScript) e §Pitfall 3 (contactOptInNote persistido no doc)
  </read_first>
  <action>
    Em convex/leads.ts:
    1. Adicionar a mutation autenticada `recordContactOptIn` (copiar o shape de recordWaOptIn, com UMA divergência deliberada: a nota é persistida NO DOC do lead, além do evento):
       ```typescript
       /** OPTIN-04: consentimento de contato generalizado (email + WhatsApp). Origem + timestamp + evento. */
       export const recordContactOptIn = mutation({
         args: {
           leadId: v.id("leads"),
           source: v.string(), // "phone_call" | "in_person" | "reply" | "other"
           note: v.optional(v.string()),
         },
         handler: async (ctx, { leadId, source, note }) => {
           const orgId = await requireOrgId(ctx);
           const lead = await ctx.db.get(leadId);
           if (!lead || lead.orgId !== orgId) throw new Error("Lead não encontrado");
           const now = Date.now();
           await ctx.db.patch(leadId, {
             contactOptInAt: now,
             contactOptInSource: source,
             ...(note ? { contactOptInNote: note } : {}),
           });
           await ctx.db.insert("events", {
             orgId,
             type: "contact_opt_in",
             leadId,
             at: now,
             meta: { source, ...(note ? { note } : {}) },
           });
           return { optInAt: now };
         },
       });
       ```
    2. Adicionar o internalMutation `setCallScript` (usado pela action outreach.callScript para persistir sem regenerar). `internalMutation` já está importado no topo do arquivo:
       ```typescript
       /** OPTIN-03: persiste o script de ligação gerado pela IA no lead. */
       export const setCallScript = internalMutation({
         args: { leadId: v.id("leads"), script: v.string(), translation: v.string() },
         handler: async (ctx, { leadId, script, translation }) => {
           await ctx.db.patch(leadId, {
             callScript: script,
             callScriptPt: translation,
             callScriptAt: Date.now(),
           });
         },
       });
       ```
    3. NÃO remover recordWaOptIn (permanece; legado). NÃO migrar dados existentes.
  </action>
  <verify>
    <automated>grep -n "export const recordContactOptIn\|export const setCallScript\|contact_opt_in\|contactOptInNote" convex/leads.ts && npx convex codegen 2>&1 | tail -2 && pnpm typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const recordContactOptIn = mutation" convex/leads.ts` casa
    - `grep "export const setCallScript = internalMutation" convex/leads.ts` casa
    - `grep 'type: "contact_opt_in"' convex/leads.ts` casa
    - `grep "contactOptInNote: note" convex/leads.ts` casa (nota persistida no doc)
    - `grep -c "export const recordWaOptIn" convex/leads.ts` retorna 1 (legado intacto)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>recordContactOptIn (grava campos no doc + evento contact_opt_in) e setCallScript (internal) presentes; recordWaOptIn intacto; typecheck verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-03-T2: writeCallScript + LANG ampliado em outreachAi.ts (com teste de paridade)</name>
  <files>convex/lib/outreachAi.ts, tests/outreach-lang.test.ts</files>
  <read_first>
    - convex/lib/outreachAi.ts (arquivo inteiro — LANG linha 5-11, SIGNAL_TEXT linha 13-20 é module-private e reusável, writeEmail linha 31-89 com o fetch/JSON.parse-fallback linha 59-88)
    - .planning/phases/04-.../04-RESEARCH.md §Pattern 3 (assinatura e prompt de writeCallScript) e §OPTIN-03
  </read_first>
  <behavior>
    - LANG contém uma entrada para CADA código em OPT_IN_MARKETS: ES→"Spanish", IT→"Italian", PT→"Portuguese", DE→"German", DK→"Danish", CH→"German"
    - LANG mantém as entradas existentes GB/IE→"English", NL→"Dutch", SE→"Swedish", NO→"Norwegian"
    - (teste puro) para todo cc em OPT_IN_MARKETS, typeof LANG[cc] === "string" && LANG[cc].length > 0
  </behavior>
  <action>
    Em convex/lib/outreachAi.ts:
    1. Ampliar o map LANG com os idiomas dos mercados opt-in (adicionar sem remover os existentes):
       ```typescript
       ES: "Spanish",
       IT: "Italian",
       PT: "Portuguese",
       DE: "German",
       DK: "Danish",
       CH: "German", // padrão suíço-alemão para B2B; a região francófona/italófona fica para depois
       ```
    2. Adicionar a função `writeCallScript`, espelhando a estrutura de writeEmail (mesmo fetch a https://api.anthropic.com/v1/messages, model "claude-sonnet-5", mesmo header/version, MESMO try/catch JSON.parse + fallback). Ela reusa SIGNAL_TEXT (module-private, mesmo arquivo) e o LANG ampliado. NÃO recebe previewUrl (o script é falado, não clicável):
       ```typescript
       /**
        * Gera um script de ligação B2B curto (~150 palavras) no idioma do mercado + tradução pt-BR.
        * O fecho pede EXPLICITAMENTE consentimento para enviar a prévia por email/WhatsApp.
        */
       export async function writeCallScript(
         apiKey: string,
         lead: Doc<"leads">,
       ): Promise<{ script: string; translation: string }> {
         const lang = LANG[lead.countryCode] ?? "English";
         const s = lead.signals;
         const pains = s
           ? (Object.keys(SIGNAL_TEXT) as (keyof Signals)[]).filter((k) => s[k]).map((k) => SIGNAL_TEXT[k])
           : [];

         const system =
           `You write a SHORT spoken cold-call opening script (~150 words) for an independent web ` +
           `professional calling a local business about their online presence, in a market where cold ` +
           `EMAIL is not legally usable without prior consent but a B2B phone call is. Write the "script" ` +
           `field in ${lang}. The call MUST: (1) open by identifying the caller, (2) name the SPECIFIC gap ` +
           `noticed, (3) mention a free preview website already built for them, (4) end by EXPLICITLY asking ` +
           `for permission to send it by email or WhatsApp. Also return a "translation" field: a faithful ` +
           `Brazilian Portuguese (pt-BR) translation of the same script, for a caller who does not speak ${lang}. ` +
           `Return STRICT JSON only: {"script": string, "translation": string}. No markdown.`;

         const user =
           `Business: ${lead.name}` +
           `${lead.city ? ` in ${lead.city}` : ""}` +
           `${lead.category ? `, a ${lead.category.replace(/_/g, " ")}` : ""}. ` +
           `Issues noticed: ${pains.length ? pains.join("; ") : "weak online presence"}. ` +
           `Write the call script and its pt-BR translation.`;

         // fetch idêntico ao writeEmail (mesmo headers/model/version); max_tokens ~800.
         // ... após parse:
         // try { const parsed = JSON.parse(text) as { script: string; translation: string };
         //   if (parsed.script && parsed.translation) return parsed; } catch {}
         // return { script: text, translation: text };
       }
       ```
       Implementar o corpo do fetch copiando EXATAMENTE o de writeEmail (linhas 59-88), trocando só o `messages`/`system`/`max_tokens` e o fallback final para `{ script: text, translation: text }`. NÃO inventar parser novo, NÃO adicionar zod.
    3. Criar tests/outreach-lang.test.ts (teste puro node:test), importando LANG de "../convex/lib/outreachAi.ts" e OPT_IN_MARKETS de "../convex/lib/domain.ts":
       ```typescript
       import { test } from "node:test";
       import assert from "node:assert/strict";
       import { LANG } from "../convex/lib/outreachAi.ts";
       import { OPT_IN_MARKETS } from "../convex/lib/domain.ts";

       test("LANG cobre todos os mercados opt-in", () => {
         for (const cc of OPT_IN_MARKETS) {
           assert.equal(typeof LANG[cc], "string");
           assert.ok(LANG[cc].length > 0, `LANG faltando para ${cc}`);
         }
       });
       test("CH usa alemão (padrão suíço-alemão B2B)", () => {
         assert.equal(LANG.CH, "German");
       });
       ```
  </action>
  <verify>
    <automated>pnpm test 2>&1 | grep -E "LANG cobre|CH usa alemão|# fail" && grep -n "export async function writeCallScript\|ES: \"Spanish\"\|PT: \"Portuguese\"\|DK: \"Danish\"\|CH: \"German\"" convex/lib/outreachAi.ts && pnpm typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function writeCallScript" convex/lib/outreachAi.ts` casa
    - `grep 'ES: "Spanish"' convex/lib/outreachAi.ts`, `'PT: "Portuguese"'`, `'DK: "Danish"'`, `'CH: "German"'` casam
    - `pnpm test` inclui "LANG cobre todos os mercados opt-in" e "CH usa alemão" passando; `# fail 0`
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>writeCallScript exportado com prompt bilíngue (script no idioma + tradução pt-BR, fecho pedindo consentimento); LANG cobre os 6 opt-in; teste de paridade verde.</done>
</task>

<task type="auto">
  <name>Task 04-03-T3: Guardrail canContactByEmail em draft/send + action callScript em outreach.ts</name>
  <files>convex/outreach.ts</files>
  <read_first>
    - convex/outreach.ts (arquivo inteiro — draft linha 137-170 com gate `if (!lead.emailable)` linha 144; send linha 248-307 com gate `if (!lead.emailable)` linha 254; imports no topo linha 1-9)
    - convex/leads.ts (recordContactOptIn/setCallScript da Task 04-03-T1 — internal.leads.setCallScript é o alvo da persistência)
    - .planning/phases/04-.../04-RESEARCH.md §Pattern 3 (action callScript) e §Pitfall 1 (sempre gatear por canContactByEmail, nunca re-derivar mercado inline) e §Anti-Patterns (callScript NÃO exige canContactByEmail — só telefone)
  </read_first>
  <action>
    Em convex/outreach.ts:
    1. No import de "./lib/domain" (hoje `import { normalizeEmail } from "./lib/domain";`), adicionar canContactByEmail:
       `import { normalizeEmail, canContactByEmail } from "./lib/domain";`
       E adicionar a importação de writeCallScript no import de "./lib/outreachAi" (hoje `import { writeEmail, LANG } from "./lib/outreachAi";`):
       `import { writeEmail, writeCallScript, LANG } from "./lib/outreachAi";`
    2. Na action `draft` (linha 144), TROCAR o gate mantendo as checagens de supressão logo abaixo intactas:
       ```typescript
       if (!canContactByEmail(lead)) {
         throw new Error("Mercado opt-in: registre o consentimento do prospect antes de enviar email.");
       }
       ```
    3. Na action `send` (linha 254), TROCAR o gate da mesma forma (a checagem de supressão isSuppressed logo abaixo permanece):
       ```typescript
       if (!canContactByEmail(lead)) {
         throw new Error("Mercado opt-in: registre o consentimento do prospect antes de enviar email.");
       }
       ```
    4. Adicionar a action `callScript` (molde: a action draft, MAS sem gate de emailabilidade/supressão e sem previewToken — gerar script para lead sem consentimento é o ponto da fase; exige só telefone + ANTHROPIC_API_KEY):
       ```typescript
       /** OPTIN-03: gera (e persiste) um script de ligação por IA. NÃO exige consentimento — só telefone. */
       export const callScript = action({
         args: { leadId: v.id("leads") },
         handler: async (ctx, { leadId }): Promise<{ script: string; translation: string }> => {
           const orgId = await requireOrgId(ctx);
           const lead = await ctx.runQuery(internal.leads.getInternal, { leadId });
           if (!lead || lead.orgId !== orgId) throw new Error("Lead não encontrado");
           if (!lead.phone) throw new Error("Lead sem telefone.");
           const key = process.env.ANTHROPIC_API_KEY;
           if (!key) throw new Error("ANTHROPIC_API_KEY não configurada no deployment Convex.");

           const result = await writeCallScript(key, lead);
           await ctx.runMutation(internal.leads.setCallScript, {
             leadId,
             script: result.script,
             translation: result.translation,
           });
           return result;
         },
       });
       ```
    5. NÃO alterar as checagens de supressão (isSuppressed) nem a injeção de rodapé/headers List-Unsubscribe da Fase 2 — elas permanecem em conjunto com o novo gate. NÃO adicionar canal "call" na tabela outreach (telefonia integrada está fora de escopo).
  </action>
  <verify>
    <automated>grep -c "!lead.emailable" convex/outreach.ts; grep -n "canContactByEmail(lead)\|Mercado opt-in: registre o consentimento\|export const callScript = action\|writeCallScript\|internal.leads.setCallScript" convex/outreach.ts; npx convex codegen 2>&1 | tail -2 && pnpm typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "!lead.emailable" convex/outreach.ts` retorna 0 (os dois gates migraram para canContactByEmail)
    - `grep -c "canContactByEmail(lead)" convex/outreach.ts` retorna 2 (draft + send)
    - `grep "Mercado opt-in: registre o consentimento do prospect antes de enviar email." convex/outreach.ts` casa
    - `grep "export const callScript = action" convex/outreach.ts` casa; `grep "internal.leads.setCallScript" convex/outreach.ts` casa
    - `grep -c "isSuppressed" convex/outreach.ts` retorna 2 (supressão em draft e send intacta)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>draft/send gateiam por canContactByEmail com erro pt-BR; supressão intacta; action callScript gera e persiste o script exigindo só telefone; typecheck verde.</done>
</task>

</tasks>

<verification>
- grep negativo: `!lead.emailable` não aparece mais em convex/outreach.ts; supressão (isSuppressed) ainda aparece 2×.
- `pnpm test` verde (incluindo o teste de paridade do LANG).
- `npx convex codegen && pnpm typecheck` verde (api inclui leads.recordContactOptIn, outreach.callScript, internal.leads.setCallScript).
- Manual (verify-work, exige ANTHROPIC_API_KEY): gerar script de lead ES → espanhol + tradução pt-BR terminando em pedido de consentimento; draft/send de lead ES sem consentimento → erro pt-BR; após recordContactOptIn → draft funciona.
</verification>

<success_criteria>
- Consentimento registrável, guardrail server-side em draft/send via canContactByEmail, e geração/persistência de script de ligação por IA — sem confiar na UI, mantendo a compliance da Fase 2 (supressão + rodapé).
</success_criteria>

<output>
After completion, create `.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-03-SUMMARY.md`
</output>
