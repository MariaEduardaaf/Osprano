---
phase: 04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/lib/domain.ts
  - convex/schema.ts
  - tests/domain.test.ts
autonomous: true
requirements: [OPTIN-01, OPTIN-04, OPTIN-06]

must_haves:
  truths:
    - "Portugal (PT) existe no MARKETS como mercado opt-in, ao lado de ES/IT/DE/DK/CH"
    - "isSearchableMarket é true para mercados launch e opt-in, false para o resto (ex.: FR)"
    - "canContactByEmail é true só quando emailable=true OU há um contactOptInAt registrado"
    - "hasWaOptIn continua true para waOptInAt legado E agora também para contactOptInAt"
    - "A invariante do isEmailable não muda: lead de mercado opt-in continua emailable=false"
    - "Os 6 mercados opt-in carregam legalReview: pending; os 5 launch não carregam a flag"
  artifacts:
    - path: "convex/lib/domain.ts"
      provides: "OPT_IN_MARKETS, SEARCHABLE_MARKETS, isSearchableMarket, canContactByEmail, hasWaOptIn (generalizado), MARKETS.PT, Market.legalReview, CITIES_BY_COUNTRY (6 chaves novas)"
      contains: "export function isSearchableMarket"
    - path: "convex/schema.ts"
      provides: "leads.contactOptInAt/Source/Note, leads.callScript/callScriptPt/callScriptAt, events.type + contact_opt_in"
      contains: "contactOptInAt"
    - path: "tests/domain.test.ts"
      provides: "casos passando para isSearchableMarket, canContactByEmail, hasWaOptIn generalizado, MARKETS.PT, legalReview"
      contains: "isSearchableMarket"
  key_links:
    - from: "tests/domain.test.ts"
      to: "convex/lib/domain.ts"
      via: "import dos novos helpers puros"
      pattern: "isSearchableMarket|canContactByEmail"
    - from: "isEmailable (domain.ts:55)"
      to: "isLaunchMarket"
      via: "chamada interna INALTERADA (regressão protegida pelo teste 'opt-in market always blocked')"
      pattern: "isLaunchMarket\\(input.countryCode\\)"
---

<objective>
Fundação do modo opt-in: separar "mercado pesquisável" de "mercado emailável", generalizar o consentimento da Fase 2 e ampliar o schema, sem tocar na regra de emailabilidade existente.

Purpose: Todos os planos seguintes (descoberta, backend de consentimento/script, UI) dependem destes helpers puros, exports e campos de schema. Esta é a única mudança da Wave 1 — tudo o mais deriva dela.
Output: `convex/lib/domain.ts` com os novos exports/helpers, `convex/schema.ts` com os campos novos, e `tests/domain.test.ts` verde com os casos novos.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-CONTEXT.md
@.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-RESEARCH.md

<interfaces>
<!-- Estado atual de convex/lib/domain.ts (o que EXISTE e NÃO pode mudar de comportamento): -->

```typescript
export type ColdEmail = "opt_out" | "conditional" | "opt_in";
export interface Market { code: string; name: string; flag: string; coldEmail: ColdEmail; }
export const MARKETS: Record<string, Market> = { GB, NL, IE, SE, NO, DE, CH, DK, IT, ES };
export const LAUNCH_MARKETS = ["GB", "NL", "IE", "SE", "NO"]; // NÃO MUDAR
export function isLaunchMarket(countryCode: string): boolean; // NÃO MUDAR
export function isEmailable(input): boolean; // linha 55 chama isLaunchMarket — NÃO MUDAR
export function hasWaOptIn(lead: { waOptInAt?: number | null }): boolean; // GENERALIZAR
export const CITIES_BY_COUNTRY: Record<string, string[]>; // GB/NL/IE/SE/NO — adicionar 6 chaves
```

<!-- Estado atual dos campos do lead em convex/schema.ts (adicionar novos opcionais, não remover): -->
```typescript
leads: { ...; emailable: v.optional(v.boolean()); waOptInAt: v.optional(v.number());
         waOptInSource: v.optional(v.string()); ... }
events: { type: v.union("preview_open","email_sent","reply","stage_change","wa_opt_in"); ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-01-T1: Novos exports e helpers puros em domain.ts (com testes)</name>
  <files>convex/lib/domain.ts, tests/domain.test.ts</files>
  <read_first>
    - convex/lib/domain.ts (arquivo inteiro — MARKETS, LAUNCH_MARKETS, isLaunchMarket, isEmailable linha 50-61, hasWaOptIn linha 68-71, CITIES_BY_COUNTRY linha 338-369)
    - tests/domain.test.ts (arquivo inteiro — estilo dos testes; o caso "isEmailable: opt-in market always blocked" linha 91-94 é regressão e NÃO pode ser alterado)
    - .planning/phases/04-.../04-RESEARCH.md §Pattern 1 (código exato proposto) e §New test cases (linhas 575-606)
  </read_first>
  <behavior>
    - isSearchableMarket("GB") === true (launch); isSearchableMarket("ES") === true, isSearchableMarket("PT") === true (opt-in); isSearchableMarket("FR") === false (mercado deferido, em nenhuma lista)
    - canContactByEmail({ emailable: true }) === true; canContactByEmail({ emailable: false }) === false; canContactByEmail({ emailable: false, contactOptInAt: Date.now() }) === true; canContactByEmail({}) === false
    - hasWaOptIn({ waOptInAt: Date.now() }) === true (legado); hasWaOptIn({ contactOptInAt: Date.now() }) === true (novo); hasWaOptIn({}) === false; hasWaOptIn({ waOptInAt: 0 }) === false
    - MARKETS.PT.coldEmail === "opt_in" e MARKETS.PT.legalReview === "pending"
    - Todos os OPT_IN_MARKETS têm MARKETS[cc].legalReview === "pending"; todos os LAUNCH_MARKETS têm MARKETS[cc].legalReview === undefined
    - REGRESSÃO: o teste existente "isEmailable: opt-in market always blocked" continua passando sem modificação
  </behavior>
  <action>
    Em convex/lib/domain.ts:
    1. Adicionar campo opcional à interface Market: `legalReview?: "pending" | "validated";` (comentar: `undefined = não aplicável (mercados launch opt-out)`).
    2. Em MARKETS, anexar `legalReview: "pending"` às 5 entradas opt-in JÁ existentes (DE, CH, DK, IT, ES) e ADICIONAR a entrada nova:
       `PT: { code: "PT", name: "Portugal", flag: "🇵🇹", coldEmail: "opt_in", legalReview: "pending" },`
       (Interpretação da pesquisa/Pitfall 6: "os 5 atuais" = os launch GB/NL/IE/SE/NO, que ficam SEM flag; "novos mercados" = todos os 6 OPT_IN_MARKETS, que nascem pending.)
    3. Logo abaixo de LAUNCH_MARKETS, adicionar:
       `export const OPT_IN_MARKETS = ["ES", "IT", "PT", "DE", "DK", "CH"];`
       `export const SEARCHABLE_MARKETS = [...LAUNCH_MARKETS, ...OPT_IN_MARKETS];`
       `export function isSearchableMarket(countryCode: string): boolean { return SEARCHABLE_MARKETS.includes(countryCode.toUpperCase()); }`
    4. NÃO tocar em LAUNCH_MARKETS, isLaunchMarket, nem isEmailable (a chamada isLaunchMarket na linha 55 é a razão de o lead opt-in ficar emailable=false — é o comportamento exigido pela OPTIN-01).
    5. Adicionar o helper puro:
       `/** OPTIN-04: consentimento explícito destrava email independente de mercado/forma jurídica. */`
       `export function canContactByEmail(lead: { emailable?: boolean | null; contactOptInAt?: number | null }): boolean { if (lead.emailable === true) return true; return typeof lead.contactOptInAt === "number" && lead.contactOptInAt > 0; }`
    6. GENERALIZAR hasWaOptIn (substituir a assinatura atual) para aceitar qualquer um dos dois timestamps — compatível com dados legados:
       `export function hasWaOptIn(lead: { waOptInAt?: number | null; contactOptInAt?: number | null }): boolean { const wa = typeof lead.waOptInAt === "number" && lead.waOptInAt > 0; const contact = typeof lead.contactOptInAt === "number" && lead.contactOptInAt > 0; return wa || contact; }`
    7. Em CITIES_BY_COUNTRY, adicionar 6 chaves novas (preservar diacríticos, como o resto do arquivo já faz com 's-Hertogenbosch/Ålesund/Tønsberg):
       `ES: ["Madrid", "Barcelona", "Valencia", "Sevilla", "Zaragoza", "Málaga", "Murcia", "Palma", "Bilbao", "Alicante", "Córdoba", "Valladolid"],`
       `IT: ["Roma", "Milano", "Napoli", "Torino", "Palermo", "Genova", "Bologna", "Firenze", "Bari", "Catania", "Venezia", "Verona"],`
       `PT: ["Lisboa", "Porto", "Vila Nova de Gaia", "Braga", "Amadora", "Setúbal", "Coimbra", "Queluz", "Funchal", "Aveiro", "Faro", "Almada"],`
       `DE: ["Berlin", "Hamburg", "München", "Köln", "Frankfurt am Main", "Stuttgart", "Düsseldorf", "Leipzig", "Dortmund", "Essen", "Bremen", "Dresden"],`
       `DK: ["København", "Aarhus", "Odense", "Aalborg", "Esbjerg", "Randers", "Kolding", "Horsens", "Vejle", "Roskilde", "Herning"],`
       `CH: ["Zürich", "Genève", "Basel", "Lausanne", "Bern", "Winterthur", "Luzern", "St. Gallen", "Lugano", "Biel/Bienne", "Thun"],`

    Em tests/domain.test.ts:
    8. Ampliar o import de "../convex/lib/domain.ts" para incluir: `isSearchableMarket, canContactByEmail, MARKETS, OPT_IN_MARKETS, LAUNCH_MARKETS` (hasWaOptIn já está importado).
    9. Adicionar os casos de teste (copiar exatamente do 04-RESEARCH.md §New test cases, linhas 577-606):
       - "isSearchableMarket: launch + opt-in markets are searchable, others are not"
       - "canContactByEmail: emailable=true always wins; contactOptInAt overrides regardless of market"
       - "hasWaOptIn: generalized to accept either waOptInAt or contactOptInAt"
       - "MARKETS.PT exists as an opt-in market with a pending legal review"
       - "all OPT_IN_MARKETS carry legalReview: pending; launch markets do not"
    10. NÃO alterar o teste existente "isEmailable: opt-in market always blocked".
  </action>
  <verify>
    <automated>pnpm test 2>&1 | grep -E "isSearchableMarket|canContactByEmail|legalReview|# fail" && grep -n "export function isSearchableMarket\|export function canContactByEmail\|OPT_IN_MARKETS\|SEARCHABLE_MARKETS" convex/lib/domain.ts && grep -c "isLaunchMarket(input.countryCode)" convex/lib/domain.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` termina com `# fail 0` (todos os novos casos passam + os 44 existentes continuam verdes)
    - `grep "export function isSearchableMarket" convex/lib/domain.ts` casa
    - `grep "export function canContactByEmail" convex/lib/domain.ts` casa
    - `grep "OPT_IN_MARKETS" convex/lib/domain.ts` casa; `grep "SEARCHABLE_MARKETS" convex/lib/domain.ts` casa
    - `grep 'PT:.*coldEmail: "opt_in".*legalReview: "pending"' convex/lib/domain.ts` casa (ou PT presente com ambos os campos)
    - `grep -c "isLaunchMarket(input.countryCode)" convex/lib/domain.ts` retorna 1 (a chamada dentro de isEmailable NÃO foi removida nem alterada)
    - `grep -c "^export const LAUNCH_MARKETS = \[\"GB\", \"NL\", \"IE\", \"SE\", \"NO\"\]" convex/lib/domain.ts` retorna 1 (LAUNCH_MARKETS intacto)
  </acceptance_criteria>
  <done>Novos exports/helpers presentes em domain.ts; isEmailable/isLaunchMarket/LAUNCH_MARKETS intactos; `pnpm test` verde incluindo os 5 casos novos e o regressão "opt-in market always blocked".</done>
</task>

<task type="auto">
  <name>Task 04-01-T2: Campos novos no schema (consentimento, script, evento)</name>
  <files>convex/schema.ts</files>
  <read_first>
    - convex/schema.ts (arquivo inteiro — tabela leads linha 57-112, tabela events linha 153-166)
    - .planning/phases/04-.../04-CONTEXT.md §OPTIN-04 e §OPTIN-03 (nomes exatos dos campos)
    - .planning/phases/04-.../04-RESEARCH.md §Pitfall 3 (contactOptInNote é campo de lead deliberadamente, diferente do waOptIn)
  </read_first>
  <action>
    Em convex/schema.ts, na tabela `leads` (adições opcionais — não remover nem alterar campos existentes; adicionar logo depois do bloco waOptInAt/waOptInSource, linha ~106):
    1. Consentimento de contato generalizado (OPTIN-04):
       `contactOptInAt: v.optional(v.number()),`
       `contactOptInSource: v.optional(v.string()), // "phone_call" | "in_person" | "reply" | "other"`
       `contactOptInNote: v.optional(v.string()), // persistido no doc (diverge do waOptIn, que só grava no evento)`
    2. Script de ligação persistido (OPTIN-03) — para não regenerar à toa:
       `callScript: v.optional(v.string()),`
       `callScriptPt: v.optional(v.string()),`
       `callScriptAt: v.optional(v.number()),`
    3. Na tabela `events`, ampliar o union `type` (linha 155-161) adicionando UM literal a mais, sem remover os existentes:
       `v.literal("contact_opt_in"),`
    Todos os campos são opcionais → mudança não-quebra (docs existentes continuam válidos). Não adicionar índices novos (nenhum acesso por índice é necessário nesta fase).
  </action>
  <verify>
    <automated>grep -n "contactOptInAt\|contactOptInSource\|contactOptInNote\|callScript\|callScriptPt\|callScriptAt\|contact_opt_in" convex/schema.ts && npx convex codegen 2>&1 | tail -3 && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "contactOptInAt: v.optional(v.number())" convex/schema.ts` casa
    - `grep "contactOptInSource: v.optional(v.string())" convex/schema.ts` casa
    - `grep "contactOptInNote: v.optional(v.string())" convex/schema.ts` casa
    - `grep "callScript: v.optional(v.string())" convex/schema.ts` e `grep "callScriptPt" convex/schema.ts` e `grep "callScriptAt" convex/schema.ts` casam
    - `grep 'v.literal("contact_opt_in")' convex/schema.ts` casa
    - `pnpm typecheck` exit 0 (schema válido; se `npx convex codegen` pedir login, o executor resolve o gate de auth e re-roda)
  </acceptance_criteria>
  <done>Campos contactOptIn* e callScript* opcionais na tabela leads; "contact_opt_in" no union de events.type; typecheck verde.</done>
</task>

</tasks>

<verification>
- `pnpm test` verde (44 antigos + 5 novos casos), incluindo a regressão "isEmailable: opt-in market always blocked" inalterada.
- `npx convex codegen && pnpm typecheck` verde (schema aceita os campos novos).
- grep negativo de segurança: `isEmailable` continua chamando `isLaunchMarket` (não foi trocado por isSearchableMarket).
</verification>

<success_criteria>
- convex/lib/domain.ts exporta OPT_IN_MARKETS, SEARCHABLE_MARKETS, isSearchableMarket, canContactByEmail; hasWaOptIn generalizado; MARKETS.PT + legalReview nos 6 opt-in; 6 chaves novas em CITIES_BY_COUNTRY.
- convex/schema.ts com contactOptIn*/callScript* opcionais no lead e "contact_opt_in" no events.type.
- LAUNCH_MARKETS/isLaunchMarket/isEmailable byte-a-byte inalterados.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-01-SUMMARY.md`
</output>
