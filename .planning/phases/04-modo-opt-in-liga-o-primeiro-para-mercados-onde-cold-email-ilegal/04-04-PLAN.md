---
phase: 04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - convex/lib/compliance.ts
  - tests/compliance.test.ts
autonomous: true
requirements: [OPTIN-04]

must_haves:
  truths:
    - "Quando um lead ES/IT/PT/DE/DK/CH é destravado por consentimento e recebe email, o rodapé de opt-out sai no idioma do mercado (não em inglês)"
    - "O idioma desconhecido continua caindo no rodapé em inglês (comportamento de fallback preservado)"
  artifacts:
    - path: "convex/lib/compliance.ts"
      provides: "FOOTER_COPY com Spanish/Italian/Portuguese/German/Danish, além dos 4 existentes"
      contains: "Spanish:"
    - path: "tests/compliance.test.ts"
      provides: "casos cobrindo o rodapé nos 5 idiomas novos"
      contains: "Spanish"
  key_links:
    - from: "convex/lib/compliance.ts (optOutFooter)"
      to: "FOOTER_COPY[lang]"
      via: "chave por nome de idioma que casa com os valores do LANG (Spanish/Italian/...)"
      pattern: "FOOTER_COPY\\[lang\\]"
---

<objective>
Localizar o rodapé de opt-out (COMP-03) para os idiomas dos mercados opt-in, para que o email destravado por consentimento saia no idioma do mercado — não em inglês.

Purpose: Ajuste incorporado pela pesquisa (04-CONTEXT.md): `FOOTER_COPY` só tinha English/Dutch/Swedish/Norwegian; um lead ES/IT/PT/DE/DK destravado por consentimento recebe o corpo no idioma certo (writeEmail usa LANG ampliado) mas cairia num rodapé em inglês. Este plano fecha essa lacuna de paridade LANG↔FOOTER_COPY.
Output: `convex/lib/compliance.ts` com 5 entradas novas em FOOTER_COPY e `tests/compliance.test.ts` verde cobrindo-as.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-CONTEXT.md
@.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-RESEARCH.md

<interfaces>
<!-- Estado atual — convex/lib/compliance.ts (FOOTER_COPY é keyed por NOME de idioma, casando com os valores do LANG de outreachAi.ts): -->
```typescript
const FOOTER_COPY: Record<string, (sender: string, url: string) => string> = {
  English: (s, url) => `\n\n—\nSent by ${s}. Don't want to hear from us again? Unsubscribe: ${url}`,
  Dutch: (s, url) => `\n\n—\nVerzonden door ${s}. Wilt u niets meer van ons ontvangen? Afmelden: ${url}`,
  Swedish: (s, url) => `\n\n—\nSkickat av ${s}. Vill du inte höra från oss igen? Avregistrera dig: ${url}`,
  Norwegian: (s, url) => `\n\n—\nSendt av ${s}. Vil du ikke høre fra oss igjen? Meld deg av: ${url}`,
};
export function optOutFooter(lang, unsubscribeUrl, senderIdentity); // fallback FOOTER_COPY.English
```
<!-- Paridade alvo: os nomes de idioma devem casar EXATAMENTE com os valores que o LANG (04-03) produz para os 6 opt-in: Spanish, Italian, Portuguese, German, Danish (CH também usa "German"). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-04-T1: FOOTER_COPY nos 5 idiomas novos (com testes)</name>
  <files>convex/lib/compliance.ts, tests/compliance.test.ts</files>
  <read_first>
    - convex/lib/compliance.ts (arquivo inteiro — FOOTER_COPY linha 6-11, optOutFooter linha 13-16)
    - tests/compliance.test.ts (arquivo inteiro — estilo dos casos optOutFooter; o caso de fallback "unknown language" deve continuar passando)
    - .planning/phases/04-.../04-RESEARCH.md §Pitfall 2 (a lacuna e por que fechá-la agora)
  </read_first>
  <behavior>
    - optOutFooter("Spanish", url, "Ana") inclui a url, o sender "Ana", começa com "\n\n—\n" e contém a palavra "baja"
    - optOutFooter("Italian", url, "Ana") contém "Annulla"
    - optOutFooter("Portuguese", url, "Ana") contém "Cancelar"
    - optOutFooter("German", url, "Ana") contém "Abmelden"
    - optOutFooter("Danish", url, "Ana") contém "Afmeld"
    - optOutFooter("Klingon", url, "Ana") ainda cai no inglês (contém a url) — fallback preservado
  </behavior>
  <action>
    Em convex/lib/compliance.ts, adicionar ao objeto FOOTER_COPY (depois de Norwegian, mesmo padrão: começa com `\n\n—\n`, cita o remetente, faz a pergunta e o verbo local de descadastro + a url). Usar EXATAMENTE estes nomes de chave (casam com os valores do LANG):
    ```typescript
    Spanish: (s, url) => `\n\n—\nEnviado por ${s}. ¿No quieres saber más de nosotros? Darse de baja: ${url}`,
    Italian: (s, url) => `\n\n—\nInviato da ${s}. Non vuoi più ricevere nostre email? Annulla l'iscrizione: ${url}`,
    Portuguese: (s, url) => `\n\n—\nEnviado por ${s}. Não quer mais receber nossos emails? Cancelar inscrição: ${url}`,
    German: (s, url) => `\n\n—\nGesendet von ${s}. Möchten Sie nichts mehr von uns hören? Abmelden: ${url}`,
    Danish: (s, url) => `\n\n—\nSendt af ${s}. Vil du ikke høre fra os igen? Afmeld dig: ${url}`,
    ```
    NÃO alterar optOutFooter nem senderIdentityFrom nem as 4 entradas existentes. (Nota: "Portuguese" aqui é o rodapé para o mercado PT — pt-PT; distinto do pt-BR usado internamente para a Duda.)

    Em tests/compliance.test.ts, adicionar casos (mesmo estilo dos existentes):
    ```typescript
    test("optOutFooter: Spanish copy", () => {
      const f = optOutFooter("Spanish", URL, "Ana");
      assert.ok(f.includes(URL)); assert.ok(f.includes("baja")); assert.ok(f.startsWith("\n\n—\n"));
    });
    test("optOutFooter: Italian copy", () => { assert.ok(optOutFooter("Italian", URL, "Ana").includes("Annulla")); });
    test("optOutFooter: Portuguese copy", () => { assert.ok(optOutFooter("Portuguese", URL, "Ana").includes("Cancelar")); });
    test("optOutFooter: German copy", () => { assert.ok(optOutFooter("German", URL, "Ana").includes("Abmelden")); });
    test("optOutFooter: Danish copy", () => { assert.ok(optOutFooter("Danish", URL, "Ana").includes("Afmeld")); });
    ```
  </action>
  <verify>
    <automated>pnpm test 2>&1 | grep -E "Spanish copy|Italian copy|Portuguese copy|German copy|Danish copy|# fail" && grep -n "Spanish:\|Italian:\|Portuguese:\|German:\|Danish:" convex/lib/compliance.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Spanish:" convex/lib/compliance.ts`, `"Italian:"`, `"Portuguese:"`, `"German:"`, `"Danish:"` todos casam
    - `pnpm test` inclui os 5 casos novos passando + o fallback "unknown language falls back to English" continua verde; `# fail 0`
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>FOOTER_COPY cobre os 9 idiomas (4 antigos + 5 novos); optOutFooter/senderIdentityFrom inalterados; testes de compliance verdes.</done>
</task>

</tasks>

<verification>
- `pnpm test` verde incluindo os 5 casos novos e o fallback de idioma desconhecido.
- Paridade: os nomes de chave (Spanish/Italian/Portuguese/German/Danish) casam com os valores que o LANG (04-03) produz para os mercados opt-in.
</verification>

<success_criteria>
- Rodapé de opt-out localizado nos idiomas dos 6 mercados opt-in; fallback inglês preservado para idioma desconhecido.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modo-opt-in-liga-o-primeiro-para-mercados-onde-cold-email-ilegal/04-04-SUMMARY.md`
</output>
