---
phase: 01-integridade-de-billing-e-seguran-a-do-modo-demo
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/model/tenant.ts
  - convex/demo.ts
  - src/proxy.ts
  - tests/tenant.test.ts
  - README.md
  - .env.example
autonomous: true
requirements: [SEC-01]
nyquist_compliant: true

user_setup:
  - service: convex-deployment
    why: "SEC-01 backend usa default-deny: o modo demo só liga quando CONVEX_ENV=development. Sem esse passo, o demo NÃO liga em nenhum deployment (é o comportamento seguro pretendido — env esquecida = demo off)."
    env_vars:
      - name: CONVEX_ENV
        source: "Rodar no deployment DEV/LOCAL para reativar o demo: `npx convex env set CONVEX_ENV development`. NUNCA setar =development num deployment de produção. (Passo operacional, não é task de código — CONTEXT.md.)"
    dashboard_config:
      - task: "Quando o deployment de PRODUÇÃO for provisionado, NÃO setar CONVEX_ENV (ou setar =production). O default-deny já mantém o demo inerte lá."
        location: "Convex deployment de produção (ainda não provisionado neste repo — ver 01-RESEARCH.md Open Questions)"

must_haves:
  truths:
    - "Com DEMO_MODE=1 mas CONVEX_ENV != development (inclusive não setada), o modo demo NÃO liga no backend (default-deny)"
    - "requireOrgId e demo.seed usam o MESMO helper isDemoEnabled (fonte única de verdade)"
    - "Com NODE_ENV=production no Next, NEXT_PUBLIC_DEMO=1 não desliga a autenticação (usa clerkProxy)"
    - "isDemoEnabled só retorna true com DEMO_MODE=1 E CONVEX_ENV=development"
  artifacts:
    - path: "convex/model/tenant.ts"
      provides: "helper isDemoEnabled (env injetável) + requireOrgId usando o helper"
      contains: "export function isDemoEnabled"
    - path: "convex/demo.ts"
      provides: "seed guardado por isDemoEnabled (importado de tenant)"
      contains: "isDemoEnabled"
    - path: "src/proxy.ts"
      provides: "demoProxy só fora de produção (NODE_ENV guard)"
      contains: "process.env.NODE_ENV"
    - path: "tests/tenant.test.ts"
      provides: "casos de isDemoEnabled (default-deny)"
      contains: "isDemoEnabled"
  key_links:
    - from: "convex/demo.ts"
      to: "isDemoEnabled"
      via: "import de ./model/tenant e guarda no início do seed"
      pattern: "isDemoEnabled\\("
    - from: "src/proxy.ts"
      to: "clerkProxy"
      via: "NEXT_PUBLIC_DEMO === '1' && NODE_ENV !== 'production' ? demoProxy : clerkProxy"
      pattern: "NODE_ENV !== .production."
    - from: "convex/model/tenant.ts:isDemoEnabled"
      to: "CONVEX_ENV"
      via: "DEMO_MODE === '1' && CONVEX_ENV === 'development'"
      pattern: "CONVEX_ENV === .development."
---

<objective>
Fechar o SEC-01: hoje os três pontos do "kill-switch" de demo (`src/proxy.ts:27`, `convex/model/tenant.ts:12`, `convex/demo.ts:133`) checam apenas a env de ativação (`NEXT_PUBLIC_DEMO`/`DEMO_MODE`), sem guarda de ambiente — uma env esquecida em produção desliga a autenticação inteira. Este plano adiciona guardas de ambiente com defesa em profundidade e centraliza a checagem backend num helper único `isDemoEnabled()`.

ATENÇÃO — mecanismo backend (decisão travada em CONTEXT.md + nota do orquestrador): o bundler do Convex substitui `process.env.NODE_ENV` pelo literal `"production"` em TODO deployment (Pitfall 1 da pesquisa), então NODE_ENV é inútil como guarda dentro de função Convex. O backend usa uma env dedicada `CONVEX_ENV` em modo **DEFAULT-DENY**: demo só liga com `CONVEX_ENV === "development"`. Deployment que não se declara dev explicitamente = seguro por padrão (demo off). O lado Next (`src/proxy.ts`) usa `NODE_ENV !== "production"`, que funciona corretamente lá.

Purpose: Impossibilitar desligar a auth por env esquecida fora de dev — requisito de segurança de produção.
Output: helper `isDemoEnabled` (default-deny) + guardas nos 3 pontos + testes + docs (README/.env.example).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-integridade-de-billing-e-seguran-a-do-modo-demo/01-CONTEXT.md
@.planning/phases/01-integridade-de-billing-e-seguran-a-do-modo-demo/01-RESEARCH.md

<interfaces>
<!-- Contratos exatos. -->

Estado atual de `convex/model/tenant.ts` (SEM guarda de ambiente):
```ts
export async function requireOrgId(ctx: QueryCtx | MutationCtx | ActionCtx): Promise<string> {
  const identity = await ctx.auth.getUserIdentity();
  if (identity) return identity.subject;
  if (process.env.DEMO_MODE === "1") return "demo";   // <- sem guarda de ambiente
  throw new Error("Não autenticado");
}
```
(Só tem `import type { QueryCtx, MutationCtx, ActionCtx }` — erased em runtime pelo --experimental-strip-types, então importável em node:test sem carregar runtime Convex.)

Estado atual de `convex/demo.ts:133` (SEM guarda de ambiente):
```ts
if (process.env.DEMO_MODE !== "1") throw new Error("DEMO_MODE desligado");
```

Estado atual de `src/proxy.ts:27` (SEM guarda de ambiente):
```ts
export default process.env.NEXT_PUBLIC_DEMO === "1" ? demoProxy : clerkProxy;
```

Padrão de teste puro para tests/tenant.test.ts:
```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { isDemoEnabled } from "../convex/model/tenant.ts";
```
</interfaces>

<decision_fidelity>
DECISÃO TRAVADA (CONTEXT.md + nota do orquestrador "usar CONVEX_ENV default-deny no backend"):
o backend usa `CONVEX_ENV === "development"` (DEFAULT-DENY). NÃO usar `CONVEX_ENV !== "production"` (default-allow) — o exemplo de código do 01-RESEARCH.md Pitfall 1 usou `!== "production"`, mas isso é DEFAULT-ALLOW e contradiz a decisão travada: uma env esquecida num prod real deixaria o demo LIGADO, exatamente a brecha que o SEC-01 fecha. Implementar default-deny: `DEMO_MODE === "1" && CONVEX_ENV === "development"`.
</decision_fidelity>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper isDemoEnabled (default-deny) + requireOrgId + testes</name>
  <files>convex/model/tenant.ts, tests/tenant.test.ts</files>
  <read_first>
    - convex/model/tenant.ts (estado atual: requireOrgId com `process.env.DEMO_MODE === "1"` sem guarda; só import type)
    - .planning/phases/01-integridade-de-billing-e-seguran-a-do-modo-demo/01-CONTEXT.md (decisão SEC-01: default-deny CONVEX_ENV === "development")
    - .planning/phases/01-integridade-de-billing-e-seguran-a-do-modo-demo/01-RESEARCH.md (Pitfall 1 — por que NODE_ENV não serve no backend Convex)
    - tests/domain.test.ts (padrão node:test, import com extensão .ts)
  </read_first>
  <behavior>
    isDemoEnabled(env = process.env): boolean — DEFAULT-DENY. Casos em tests/tenant.test.ts:
    - isDemoEnabled({ DEMO_MODE: "1", CONVEX_ENV: "development" }) === true
    - isDemoEnabled({ DEMO_MODE: "1", CONVEX_ENV: undefined }) === false   (default-deny: env esquecida = off)
    - isDemoEnabled({ DEMO_MODE: "1", CONVEX_ENV: "production" }) === false
    - isDemoEnabled({ DEMO_MODE: undefined, CONVEX_ENV: "development" }) === false
    - isDemoEnabled({ DEMO_MODE: "0", CONVEX_ENV: "development" }) === false
  </behavior>
  <action>
    Escrever os testes PRIMEIRO (RED), depois implementar (GREEN).

    1. `convex/model/tenant.ts` — adicionar o helper com env injetável (para testabilidade) usando DEFAULT-DENY:
    ```ts
    /** O modo demo só liga fora de produção. DEFAULT-DENY: exige CONVEX_ENV="development"
     *  explícito (env esquecida = demo off = seguro). NODE_ENV NÃO serve aqui — o bundler
     *  do Convex o fixa em "production" em todo deployment (ver 01-RESEARCH.md, Pitfall 1). */
    export function isDemoEnabled(
      env: Pick<NodeJS.ProcessEnv, "DEMO_MODE" | "CONVEX_ENV"> = process.env,
    ): boolean {
      return env.DEMO_MODE === "1" && env.CONVEX_ENV === "development";
    }
    ```

    2. `requireOrgId` — trocar a checagem inline por `isDemoEnabled()`:
    ```ts
    export async function requireOrgId(ctx: QueryCtx | MutationCtx | ActionCtx): Promise<string> {
      const identity = await ctx.auth.getUserIdentity();
      if (identity) return identity.subject;
      // Demo mode (dev only): workspace "demo" compartilhado, sem auth.
      if (isDemoEnabled()) return "demo";
      throw new Error("Não autenticado");
    }
    ```

    3. `tests/tenant.test.ts` (novo) — cobrir os casos do <behavior>. Import direto de `../convex/model/tenant.ts` (só import type de _generated → erased em runtime, funciona em node:test). O glob `tests/*.test.ts` já detecta o arquivo.

    CRÍTICO: usar `=== "development"` (default-deny), NÃO `!== "production"`. Ver <decision_fidelity> no contexto.
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function isDemoEnabled" convex/model/tenant.ts` retorna 0
    - `grep -q 'CONVEX_ENV === "development"' convex/model/tenant.ts` retorna 0
    - `grep -q 'CONVEX_ENV !== "production"' convex/model/tenant.ts` retorna 1 (NÃO deve existir — default-allow proibido)
    - `grep -q "if (isDemoEnabled())" convex/model/tenant.ts` retorna 0 (requireOrgId usa o helper)
    - `test -f tests/tenant.test.ts` verdadeiro e `grep -q "isDemoEnabled" tests/tenant.test.ts` retorna 0
    - `pnpm test` passa: isDemoEnabled true SÓ com DEMO_MODE=1 E CONVEX_ENV=development; false com CONVEX_ENV undefined
    - `pnpm typecheck` sai com exit 0
  </acceptance_criteria>
  <done>isDemoEnabled existe em modo default-deny (CONVEX_ENV === "development"), requireOrgId o usa, e os testes verdes provam que env esquecida = demo off.</done>
</task>

<task type="auto">
  <name>Task 2: Guardas em demo.ts e proxy.ts + documentação</name>
  <files>convex/demo.ts, src/proxy.ts, README.md, .env.example</files>
  <read_first>
    - convex/demo.ts (linha 133: `if (process.env.DEMO_MODE !== "1") throw ...` — trocar por isDemoEnabled)
    - convex/model/tenant.ts (isDemoEnabled — criado na Task 1, exportado)
    - src/proxy.ts (linha 27: export default ternário sem guarda de ambiente)
    - README.md (seção "🧪 Modo demo" nas linhas ~92-108 — atualizar para refletir dev-only + CONVEX_ENV)
    - .env.example (linhas 20-31, bloco de server-side keys do Convex — documentar CONVEX_ENV)
  </read_first>
  <action>
    Aplicar a guarda de ambiente nos dois pontos restantes e documentar o passo operacional do CONVEX_ENV.

    1. `convex/demo.ts` — importar `isDemoEnabled` de `./model/tenant` e trocar a guarda do `seed`:
    ```ts
    import { isDemoEnabled } from "./model/tenant";
    // ...
    // linha 133, dentro do handler de seed:
    if (!isDemoEnabled()) throw new Error("DEMO_MODE desligado");
    ```

    2. `src/proxy.ts` — adicionar a guarda NODE_ENV (aqui NODE_ENV FUNCIONA — é o valor real do processo Next, confirmado no 01-RESEARCH.md Pattern 3):
    ```ts
    export default process.env.NEXT_PUBLIC_DEMO === "1" && process.env.NODE_ENV !== "production"
      ? demoProxy
      : clerkProxy;
    ```
    (O ternário é avaliado uma vez na carga do módulo — seguro; NODE_ENV é estável durante todo o processo.)

    3. `README.md` — na seção "🧪 Modo demo (sem Clerk, com dados de exemplo)" (~linha 92), atualizar o bloco de comandos e a nota para refletir que o demo agora exige CONVEX_ENV=development no backend e só roda fora de produção. Adicionar ao bloco `npx convex env set`:
    ```bash
    npx convex env set DEMO_MODE 1
    npx convex env set CONVEX_ENV development   # obrigatório: o demo é default-deny (só liga fora de produção)
    ```
    E na nota, deixar claro: "O modo demo é inerte em produção — em qualquer deployment sem `CONVEX_ENV=development` a auth permanece ativa (segurança por padrão). No Next, `NEXT_PUBLIC_DEMO=1` só tem efeito com `NODE_ENV` de desenvolvimento."

    4. `.env.example` — no bloco de comentários de server-side keys do Convex (linhas 20-31), adicionar a linha:
    ```
    #   CONVEX_ENV                 — setar "development" SÓ no deployment dev para ligar o modo demo (default-deny; nunca "development" em produção)
    ```

    Tom pt-BR seguindo o existente. NÃO rodar `npx convex env set` (é passo operacional da usuária — ver user_setup no frontmatter).
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -20 && pnpm lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import { isDemoEnabled } from "./model/tenant"' convex/demo.ts` retorna 0
    - `grep -q "if (!isDemoEnabled())" convex/demo.ts` retorna 0
    - `grep -q 'process.env.DEMO_MODE !== "1"' convex/demo.ts` retorna 1 (guarda antiga removida)
    - `grep -q 'process.env.NODE_ENV !== "production"' src/proxy.ts` retorna 0
    - `grep -q "NEXT_PUBLIC_DEMO === \"1\" && process.env.NODE_ENV !== \"production\"" src/proxy.ts` retorna 0
    - `grep -q "CONVEX_ENV development" README.md` retorna 0 (comando documentado)
    - `grep -q "CONVEX_ENV" .env.example` retorna 0
    - `pnpm typecheck` exit 0 e `pnpm lint` sem erros
    - `pnpm test` continua verde
  </acceptance_criteria>
  <done>demo.seed e proxy usam a guarda de ambiente (backend via isDemoEnabled/CONVEX_ENV, Next via NODE_ENV); README e .env.example documentam o passo operacional; typecheck/lint verdes.</done>
</task>

</tasks>

<verification>
- `pnpm test` verde (casos de isDemoEnabled provando default-deny)
- `pnpm typecheck` exit 0; `pnpm lint` sem erros
- `grep` das guardas nos 3 pontos (tenant.ts, demo.ts, proxy.ts)
- Verificação manual (env-dependente, sem harness): `NODE_ENV=production pnpm build && pnpm start` com `NEXT_PUBLIC_DEMO=1` deve exigir login em rota protegida (proxy usa clerkProxy). Backend: sem CONVEX_ENV=development, `demo:seed` lança "DEMO_MODE desligado" mesmo com DEMO_MODE=1
</verification>

<success_criteria>
- SEC-01: com CONVEX_ENV != "development" (inclusive não setada), DEMO_MODE=1 não ativa o demo nem o seed no backend; com NODE_ENV=production, NEXT_PUBLIC_DEMO=1 não desliga a auth no Next
- Fonte única de verdade no backend: requireOrgId e demo.seed usam isDemoEnabled
- Passo operacional documentado (README + .env.example): setar CONVEX_ENV=development só no deployment dev
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/01-integridade-de-billing-e-seguran-a-do-modo-demo/01-03-SUMMARY.md`
</output>
